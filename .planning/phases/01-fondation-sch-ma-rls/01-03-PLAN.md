---
phase: 01-fondation-sch-ma-rls
plan: 03
type: execute
wave: 2
depends_on: [01-02]
files_modified:
  - supabase/migrations/20260316000005_create_dossiers.sql
  - supabase/migrations/20260316000006_create_rls_helper_functions.sql
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03]

must_haves:
  truths:
    - "La table dossiers existe avec les colonnes AGO-spécifiques toutes nullables (date_cloture, date_echeance, id_pennylane, forme_juridique, siren, regime_fiscal, statut_exercice_pl)"
    - "dossiers.task_uid a une contrainte UNIQUE — clé de déduplication Pennylane"
    - "dossiers.status_id est une FK vers org_statuses(id) avec ON DELETE RESTRICT (pas NO ACTION)"
    - "Les fonctions private.get_my_org_id(), private.get_my_role(), private.get_my_cabinet_ids() existent dans le schéma private"
    - "Ces fonctions sont SECURITY DEFINER et STABLE"
    - "private.get_my_cabinet_ids() retourne uuid[] via array_agg sur user_cabinet_assignments WHERE user_id = (select auth.uid())"
  artifacts:
    - path: "supabase/migrations/20260316000005_create_dossiers.sql"
      provides: "Table dossiers avec FK status_id ON DELETE RESTRICT et champs AGO nullables"
      exports: []
    - path: "supabase/migrations/20260316000006_create_rls_helper_functions.sql"
      provides: "Schéma private + 3 fonctions helper SECURITY DEFINER pour les politiques RLS"
      exports: ["private.get_my_org_id()", "private.get_my_role()", "private.get_my_cabinet_ids()"]
  key_links:
    - from: "supabase/migrations/20260316000005_create_dossiers.sql"
      to: "supabase/migrations/20260316000004_create_org_statuses.sql"
      via: "dossiers.status_id REFERENCES public.org_statuses(id) ON DELETE RESTRICT"
      pattern: "on delete restrict"
    - from: "supabase/migrations/20260316000006_create_rls_helper_functions.sql"
      to: "public.user_cabinet_assignments"
      via: "get_my_cabinet_ids() lit user_cabinet_assignments WHERE user_id = (select auth.uid())"
      pattern: "from public\\.user_cabinet_assignments"
    - from: "supabase/migrations/20260316000006_create_rls_helper_functions.sql"
      to: "auth.jwt()"
      via: "get_my_org_id() et get_my_role() lisent auth.jwt()->'app_metadata'"
      pattern: "auth\\.jwt\\(\\)"

interfaces:
  # Ces fonctions sont utilisées directement dans les politiques RLS du Plan 04
  # L'executor du Plan 04 DOIT utiliser exactement ces signatures

  # private.get_my_org_id() → uuid
  # Retourne l'org_id depuis JWT app_metadata. NULL si JWT absent ou claim manquant.
  # Usage dans RLS : organization_id = (select private.get_my_org_id())

  # private.get_my_role() → text
  # Retourne le role depuis JWT app_metadata. Valeurs possibles : 'expert-comptable', 'collaborateur'.
  # Usage dans RLS : (select private.get_my_role()) = 'expert-comptable'

  # private.get_my_cabinet_ids() → uuid[]
  # Retourne le tableau des cabinet_id assignés à l'utilisateur courant.
  # Usage dans RLS : cabinet_id = any(select private.get_my_cabinet_ids())
---

<objective>
Créer la table dossiers (avec FK ON DELETE RESTRICT vers org_statuses) et les 3 fonctions helper SECURITY DEFINER dans le schéma private qui alimenteront toutes les politiques RLS du Plan 04.

Purpose: La table dossiers est la table centrale du produit. Les fonctions helper encapsulent la lecture des JWT claims pour les politiques RLS — sans elles, les politiques seraient répétitives et lentes.
Output: Migration 005 (table dossiers) + Migration 006 (schéma private + 3 fonctions helper).
</objective>

<execution_context>
@/Users/zakariabillouche/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zakariabillouche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-fondation-sch-ma-rls/01-CONTEXT.md
@.planning/phases/01-fondation-sch-ma-rls/01-RESEARCH.md
@.planning/phases/01-fondation-sch-ma-rls/01-01-SUMMARY.md
@.planning/phases/01-fondation-sch-ma-rls/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration dossiers avec champs AGO nullables et FK RESTRICT</name>
  <files>supabase/migrations/20260316000005_create_dossiers.sql</files>
  <read_first>
    - .planning/phases/01-fondation-sch-ma-rls/01-CONTEXT.md (§Structure des tables — dossiers : colonnes exactes, §Champs AGO-spécifiques : liste complète avec note "tous nullables")
    - .planning/phases/01-fondation-sch-ma-rls/01-RESEARCH.md (§Common Pitfalls — Pitfall 3 : ON DELETE RESTRICT vs NO ACTION, Pitfall 5 : ordre des migrations)
  </read_first>
  <action>
Créer `supabase/migrations/20260316000005_create_dossiers.sql` :

```sql
-- Migration 005: Create dossiers table
-- Phase 1 — Fondation : Schéma & RLS
-- FOUND-01 : champs AGO-spécifiques nullables — un dossier de type différent peut être inséré sans ces champs
-- FOUND-05 : status_id FK avec ON DELETE RESTRICT (pas NO ACTION) — immédiat, non-différable

create table public.dossiers (
  id              uuid primary key default gen_random_uuid(),
  organization_id uuid not null references public.organizations(id) on delete cascade,
  cabinet_id      uuid not null references public.cabinets(id) on delete cascade,
  type            text not null,           -- ex : 'AGO', 'création de société' (extensible, pas d'enum)
  task_uid        text unique,             -- clé de déduplication Pennylane — UNIQUE, nullable pour dossiers manuels
  status_id       uuid not null references public.org_statuses(id) on delete restrict,
  -- Champs AGO-spécifiques : TOUS NULLABLES — n'empêchent pas l'insertion d'autres types de missions
  date_cloture        date,
  date_echeance       date,
  id_pennylane        text,
  forme_juridique     text,
  siren               text,
  regime_fiscal       text,
  statut_exercice_pl  text,
  -- Timestamps
  created_at      timestamptz not null default now(),
  updated_at      timestamptz not null default now()
);

-- Index pour les politiques RLS (colonnes filtrées dans chaque politique)
create index idx_dossiers_organization_id on public.dossiers(organization_id);
create index idx_dossiers_cabinet_id      on public.dossiers(cabinet_id);
create index idx_dossiers_status_id       on public.dossiers(status_id);

-- Trigger pour updated_at automatique
create or replace function public.update_updated_at()
returns trigger
language plpgsql
as $$
begin
  NEW.updated_at = now();
  return NEW;
end;
$$;

create trigger trg_dossiers_updated_at
before update on public.dossiers
for each row execute function public.update_updated_at();

comment on table public.dossiers is 'Entité centrale — une mission juridique (AGO ou autre type)';
comment on column public.dossiers.type is 'Type de mission — non contraint par enum pour extensibilité v2 (FOUND-01)';
comment on column public.dossiers.task_uid is 'Clé de déduplication Pennylane — UNIQUE, nullable pour dossiers créés manuellement';
comment on column public.dossiers.status_id is 'FK avec ON DELETE RESTRICT — suppression d''un statut utilisé bloquée au niveau DB (FOUND-05)';
comment on column public.dossiers.date_cloture is 'Champ AGO-spécifique nullable — FOUND-01';
comment on column public.dossiers.date_echeance is 'Champ AGO-spécifique nullable — deadline affichée avec badge urgence en Phase 3';
```

Points critiques :
- `type` est TEXT (pas un enum) — extensible pour les types v2 sans migration destructive (FOUND-01)
- `on delete restrict` sur status_id (pas `on delete no action` — différence subtile expliquée dans RESEARCH.md Pitfall 3)
- Les 7 champs AGO-spécifiques n'ont AUCUNE contrainte NOT NULL — nullables sans défaut
- `task_uid` est UNIQUE mais nullable (un dossier manuel sans sync Pennylane peut ne pas avoir de task_uid)
  </action>
  <verify>
    <automated>grep -E "on delete restrict|date_cloture\s+date|type\s+text not null|task_uid\s+text unique" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/20260316000005_create_dossiers.sql</automated>
  </verify>
  <done>Le fichier contient "on delete restrict" (pour status_id), "date_cloture" sans NOT NULL, "type text not null" (type extensible), et "task_uid text unique".</done>
</task>

<task type="auto">
  <name>Task 2: Migration fonctions helper RLS dans schéma private</name>
  <files>supabase/migrations/20260316000006_create_rls_helper_functions.sql</files>
  <read_first>
    - .planning/phases/01-fondation-sch-ma-rls/01-RESEARCH.md (§Pattern 1 — Fonctions Helper SECURITY DEFINER : code SQL complet vérifié, §State of the Art — "Fonctions helper dans schéma private non-exposé", §Anti-Patterns — "auth.uid() sans wrapping")
    - .planning/phases/01-fondation-sch-ma-rls/01-CONTEXT.md (§Politiques RLS — "fonctions helper get_my_org_id(), get_my_role(), get_my_cabinet_ids()", §JWT Custom Claims — "auth.jwt()->'app_metadata'")
  </read_first>
  <action>
Créer `supabase/migrations/20260316000006_create_rls_helper_functions.sql` :

```sql
-- Migration 006: Create RLS helper functions in private schema
-- Phase 1 — Fondation : Schéma & RLS
-- FOUND-02 : isolation multi-tenant + FOUND-03 : JWT custom claims via app_metadata
--
-- Toutes les fonctions sont :
--   - SECURITY DEFINER : s'exécutent avec les droits du créateur (bypass RLS sur tables internes)
--   - STABLE : PostgreSQL peut les mettre en cache (optimisation initPlan)
--   - Dans schéma private : non exposées via PostgREST — empêche l'appel direct par les clients
--
-- Usage dans les politiques RLS : toujours avec (select private.fn()) pour forcer le cache initPlan
-- Benchmark Supabase : jusqu'à 95% d'amélioration vs appel direct sans wrapping select

-- Créer le schéma private (non exposé via PostgREST par défaut)
create schema if not exists private;

-- Fonction 1 : retourne l'org_id de l'utilisateur courant depuis JWT app_metadata
-- Retourne NULL si le JWT est absent, malformé, ou si le claim org_id est manquant
create or replace function private.get_my_org_id()
returns uuid
language sql
security definer
stable
as $$
  select (auth.jwt() -> 'app_metadata' ->> 'org_id')::uuid
$$;

-- Fonction 2 : retourne le role de l'utilisateur courant depuis JWT app_metadata
-- Valeurs attendues : 'expert-comptable' ou 'collaborateur'
-- Retourne NULL si le JWT est absent ou si le claim role est manquant
create or replace function private.get_my_role()
returns text
language sql
security definer
stable
as $$
  select auth.jwt() -> 'app_metadata' ->> 'role'
$$;

-- Fonction 3 : retourne le tableau des cabinet_id assignés à l'utilisateur courant
-- Lit public.user_cabinet_assignments via SECURITY DEFINER (bypass RLS sur cette table)
-- Le wrapping (select auth.uid()) force le cache initPlan pour auth.uid()
-- Retourne un tableau vide (pas NULL) si aucun cabinet assigné
create or replace function private.get_my_cabinet_ids()
returns uuid[]
language sql
security definer
stable
as $$
  select coalesce(array_agg(cabinet_id), '{}')
  from public.user_cabinet_assignments
  where user_id = (select auth.uid())
$$;

comment on function private.get_my_org_id() is 'RLS helper — lit org_id depuis JWT app_metadata. Usage : (select private.get_my_org_id())';
comment on function private.get_my_role() is 'RLS helper — lit role depuis JWT app_metadata. Usage : (select private.get_my_role())';
comment on function private.get_my_cabinet_ids() is 'RLS helper — retourne uuid[] des cabinets assignés. Usage : cabinet_id = any(select private.get_my_cabinet_ids())';
```

Points critiques :
- Les fonctions DOIVENT être dans le schéma `private` (pas `public`) — sinon elles sont exposées via PostgREST
- `SECURITY DEFINER` est obligatoire — sans lui, la lecture de `user_cabinet_assignments` dans `get_my_cabinet_ids()` créerait une récursion RLS (RESEARCH.md Pitfall 4)
- `STABLE` permet à PostgreSQL de cacher le résultat dans une même requête
- `coalesce(array_agg(cabinet_id), '{}')` retourne un tableau vide plutôt que NULL si aucun cabinet assigné — évite les comparaisons `= ANY(NULL)` qui sont toujours fausses
- Le wrapping `(select auth.uid())` dans `get_my_cabinet_ids()` force le cache initPlan pour `auth.uid()`
  </action>
  <verify>
    <automated>grep -E "create schema if not exists private|security definer|coalesce\(array_agg|auth\.jwt\(\) -> 'app_metadata'" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/20260316000006_create_rls_helper_functions.sql</automated>
  </verify>
  <done>Le fichier contient "create schema if not exists private", "security definer" (au moins 3 fois), "coalesce(array_agg", et "auth.jwt() -> 'app_metadata'".</done>
</task>

</tasks>

<verification>
```bash
# Vérifier les 2 nouvelles migrations
ls /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/20260316000005* /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/20260316000006*

# FOUND-01 : 7 champs AGO nullables dans dossiers
grep -E "date_cloture|date_echeance|id_pennylane|forme_juridique|siren|regime_fiscal|statut_exercice_pl" \
  /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/20260316000005_create_dossiers.sql | wc -l
# Doit retourner 7

# FOUND-05 : ON DELETE RESTRICT (pas NO ACTION)
grep "on delete restrict" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/20260316000005_create_dossiers.sql

# FOUND-03 : 3 fonctions dans schéma private
grep "create or replace function private\." /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/20260316000006_create_rls_helper_functions.sql | wc -l
# Doit retourner 3
```
</verification>

<success_criteria>
- Migration 005 : table dossiers avec type TEXT (pas enum), 7 champs AGO tous nullables sans NOT NULL, status_id FK avec ON DELETE RESTRICT, task_uid UNIQUE nullable, index sur organization_id/cabinet_id/status_id
- Migration 006 : schéma private créé, 3 fonctions SECURITY DEFINER STABLE dans private (get_my_org_id, get_my_role, get_my_cabinet_ids), get_my_cabinet_ids utilise coalesce pour retourner [] plutôt que NULL
- Le Plan 04 peut utiliser les signatures exactes : `(select private.get_my_org_id())`, `(select private.get_my_role())`, `cabinet_id = any(select private.get_my_cabinet_ids())`
</success_criteria>

<output>
After completion, create `.planning/phases/01-fondation-sch-ma-rls/01-03-SUMMARY.md`
</output>
