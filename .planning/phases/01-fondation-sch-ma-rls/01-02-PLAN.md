---
phase: 01-fondation-sch-ma-rls
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260316000001_create_organizations.sql
  - supabase/migrations/20260316000002_create_profiles_cabinets.sql
  - supabase/migrations/20260316000003_create_user_cabinet_assignments.sql
  - supabase/migrations/20260316000004_create_org_statuses.sql
autonomous: true
requirements: [FOUND-04, FOUND-05]

must_haves:
  truths:
    - "La table organizations existe avec les colonnes : id (uuid pk), name (text not null), plan (enum starter/pro/cabinet/enterprise), active_dossiers_count (int default 0), created_at"
    - "La table profiles existe et étend auth.users avec les colonnes : id (= auth.users.id), organization_id, role (enum expert-comptable/collaborateur), first_name, last_name, phone, avatar_url, created_at"
    - "La table cabinets existe avec les colonnes : id, organization_id (FK organizations), name (not null), code (nullable), created_at"
    - "La table user_cabinet_assignments existe avec les colonnes : user_id, cabinet_id, assigned_at — clé primaire composite (user_id, cabinet_id)"
    - "La table org_statuses existe avec les colonnes : id, organization_id, label, type (enum normal/terminal), display_order (int), created_at"
    - "Un trigger AFTER INSERT sur organizations pré-charge exactement 13 statuts avec les labels et display_order prescrits"
    - "La FK dossiers.status_id → org_statuses.id utilise ON DELETE RESTRICT (pas NO ACTION)"
  artifacts:
    - path: "supabase/migrations/20260316000001_create_organizations.sql"
      provides: "Table organizations avec enum plan et colonne active_dossiers_count default 0"
    - path: "supabase/migrations/20260316000002_create_profiles_cabinets.sql"
      provides: "Table profiles (liée auth.users) + table cabinets"
    - path: "supabase/migrations/20260316000003_create_user_cabinet_assignments.sql"
      provides: "Table de jonction user_cabinet_assignments avec PK composite"
    - path: "supabase/migrations/20260316000004_create_org_statuses.sql"
      provides: "Table org_statuses + fonction seed_default_statuses() + trigger trg_seed_org_statuses"
  key_links:
    - from: "supabase/migrations/20260316000002_create_profiles_cabinets.sql"
      to: "auth.users"
      via: "profiles.id REFERENCES auth.users(id) ON DELETE CASCADE"
      pattern: "references auth\\.users"
    - from: "supabase/migrations/20260316000004_create_org_statuses.sql"
      to: "organizations"
      via: "trigger AFTER INSERT sur organizations appelle seed_default_statuses()"
      pattern: "after insert on public\\.organizations"
---

<objective>
Créer les 4 premières migrations Supabase qui établissent le schéma de base : organizations, profiles, cabinets, user_cabinet_assignments, org_statuses, et le trigger de pré-chargement des 13 statuts par défaut.

Purpose: Poser la fondation du schéma multi-tenant dans l'ordre de dépendances correct — organizations en premier, org_statuses en dernier dans ce lot (dossiers et ses FK viennent au Plan 03).
Output: 4 fichiers SQL de migration versionnés dans supabase/migrations/, prêts pour `supabase migration up`.
</objective>

<execution_context>
@/Users/zakariabillouche/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zakariabillouche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-fondation-sch-ma-rls/01-CONTEXT.md
@.planning/phases/01-fondation-sch-ma-rls/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration organizations (table + enum plan)</name>
  <files>supabase/migrations/20260316000001_create_organizations.sql</files>
  <read_first>
    - .planning/phases/01-fondation-sch-ma-rls/01-CONTEXT.md (§Structure des tables — organizations : colonnes exactes décidées)
    - .planning/phases/01-fondation-sch-ma-rls/01-RESEARCH.md (§Claude's Discretion Recommendations — colonnes Stripe : NE PAS les ajouter en Phase 1)
  </read_first>
  <action>
Créer `supabase/migrations/20260316000001_create_organizations.sql` avec le contenu suivant :

```sql
-- Migration 001: Create organizations table
-- Phase 1 — Fondation : Schéma & RLS
-- Colonnes Stripe (subscription_id, customer_id) sont hors scope — voir Phase 7

-- Enum pour le plan d'abonnement
create type public.plan_type as enum ('starter', 'pro', 'cabinet', 'enterprise');

-- Table organizations : entité racine du multi-tenant
create table public.organizations (
  id                  uuid primary key default gen_random_uuid(),
  name                text not null,
  plan                public.plan_type not null default 'starter',
  active_dossiers_count integer not null default 0,
  created_at          timestamptz not null default now()
);

-- Index pour les requêtes RLS (colonnes filtrées fréquemment)
create index idx_organizations_id on public.organizations(id);

comment on table public.organizations is 'Entité racine multi-tenant — un groupe comptable';
comment on column public.organizations.active_dossiers_count is 'Mise à jour par trigger (Phase 5/7) — initialisé à 0';
comment on column public.organizations.plan is 'Plan actuel — starter par défaut, modifié par les webhooks Stripe (Phase 7)';
```

Points critiques :
- `plan` est un enum PostgreSQL (pas text) — les valeurs exactes sont : 'starter', 'pro', 'cabinet', 'enterprise'
- `active_dossiers_count` a un default 0 et NOT NULL — le trigger de mise à jour vient en Phase 5/7, pas maintenant
- PAS de colonnes Stripe (subscription_id, customer_id, stripe_customer_id) — ces colonnes sont déférées en Phase 7 (décision CONTEXT.md)
  </action>
  <verify>
    <automated>grep -E "plan_type|active_dossiers_count|create table public\.organizations" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/20260316000001_create_organizations.sql</automated>
  </verify>
  <done>Le fichier contient "create type public.plan_type as enum", "create table public.organizations", "active_dossiers_count integer not null default 0", et n'inclut PAS "subscription_id" ni "customer_id".</done>
</task>

<task type="auto">
  <name>Task 2: Migrations profiles et cabinets</name>
  <files>supabase/migrations/20260316000002_create_profiles_cabinets.sql</files>
  <read_first>
    - .planning/phases/01-fondation-sch-ma-rls/01-CONTEXT.md (§Structure des tables — profiles : "id (= auth.users.id)", cabinets : colonnes exactes)
    - .planning/phases/01-fondation-sch-ma-rls/01-RESEARCH.md (§Anti-Patterns — "raw_user_meta_data pour les rôles" à éviter)
  </read_first>
  <action>
Créer `supabase/migrations/20260316000002_create_profiles_cabinets.sql` :

```sql
-- Migration 002: Create profiles and cabinets tables
-- Phase 1 — Fondation : Schéma & RLS

-- Enum pour le rôle utilisateur
create type public.user_role as enum ('expert-comptable', 'collaborateur');

-- Table profiles : étend auth.users avec les données métier
-- profiles.id = auth.users.id (FK, pas une PK générée)
create table public.profiles (
  id              uuid primary key references auth.users(id) on delete cascade,
  organization_id uuid references public.organizations(id) on delete cascade,
  role            public.user_role,
  first_name      text,
  last_name       text,
  phone           text,
  avatar_url      text,
  created_at      timestamptz not null default now()
);

-- Index pour les politiques RLS (colonnes filtrées fréquemment)
create index idx_profiles_organization_id on public.profiles(organization_id);
create index idx_profiles_id on public.profiles(id);

comment on table public.profiles is 'Extension de auth.users — données métier utilisateur';
comment on column public.profiles.organization_id is 'NULL avant que l''utilisateur complète son profil (Phase 2)';
comment on column public.profiles.role is 'NULL avant onboarding — assigné lors de la création org ou de l''invitation (Phase 2)';

-- Table cabinets : sous-entité organisationnelle d'un groupe comptable
create table public.cabinets (
  id              uuid primary key default gen_random_uuid(),
  organization_id uuid not null references public.organizations(id) on delete cascade,
  name            text not null,
  code            text,           -- référence interne libre, optionnelle
  created_at      timestamptz not null default now()
);

-- Index pour les politiques RLS
create index idx_cabinets_organization_id on public.cabinets(organization_id);

comment on table public.cabinets is 'Entité cabinet/bureau dans une organisation';
comment on column public.cabinets.code is 'Référence interne libre (optionnelle) — ex : code Pennylane';
```

Points critiques :
- `profiles.id` est une FK vers `auth.users(id)` avec `ON DELETE CASCADE` — quand l'utilisateur Supabase est supprimé, son profil est supprimé
- `organization_id` et `role` dans profiles sont NULLABLE — ils sont remplis lors de l'onboarding (Phase 2), pas à la création du compte
- `cabinets.code` est nullable (text sans NOT NULL) — référence interne optionnelle
  </action>
  <verify>
    <automated>grep -E "references auth\.users\(id\)|create table public\.cabinets|idx_profiles_organization_id" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/20260316000002_create_profiles_cabinets.sql</automated>
  </verify>
  <done>Le fichier contient "references auth.users(id) on delete cascade", "create table public.cabinets", "idx_profiles_organization_id", et profiles.organization_id est NULLABLE (pas de NOT NULL).</done>
</task>

<task type="auto">
  <name>Task 3: Migrations user_cabinet_assignments et org_statuses + trigger seed</name>
  <files>supabase/migrations/20260316000003_create_user_cabinet_assignments.sql, supabase/migrations/20260316000004_create_org_statuses.sql</files>
  <read_first>
    - .planning/phases/01-fondation-sch-ma-rls/01-CONTEXT.md (§user_cabinet_assignments : "clé de la RLS collaborateur", §Statuts pré-chargés — liste exacte des 13 statuts avec labels et ordre)
    - .planning/phases/01-fondation-sch-ma-rls/01-RESEARCH.md (§Pattern 3 — Trigger de pré-chargement, code SQL complet vérifié)
  </read_first>
  <action>
Créer `supabase/migrations/20260316000003_create_user_cabinet_assignments.sql` :

```sql
-- Migration 003: Create user_cabinet_assignments join table
-- Phase 1 — Fondation : Schéma & RLS
-- Table pivot clé pour la RLS collaborateur — get_my_cabinet_ids() la lit

create table public.user_cabinet_assignments (
  user_id     uuid not null references public.profiles(id) on delete cascade,
  cabinet_id  uuid not null references public.cabinets(id) on delete cascade,
  assigned_at timestamptz not null default now(),
  primary key (user_id, cabinet_id)
);

-- Index pour la fonction helper get_my_cabinet_ids() — filtre sur user_id
create index idx_user_cabinet_assignments_user_id on public.user_cabinet_assignments(user_id);
create index idx_user_cabinet_assignments_cabinet_id on public.user_cabinet_assignments(cabinet_id);

comment on table public.user_cabinet_assignments is 'Assignation collaborateur → cabinets — lue par la fonction RLS get_my_cabinet_ids()';
```

Créer `supabase/migrations/20260316000004_create_org_statuses.sql` :

```sql
-- Migration 004: Create org_statuses table + trigger seed
-- Phase 1 — Fondation : Schéma & RLS
-- FOUND-04 : statuts pré-chargés à la création de l'organisation (13 statuts)
-- FOUND-05 : FK dossiers.status_id → org_statuses(id) ON DELETE RESTRICT (dans Migration 005)

-- Enum pour le type de statut
create type public.status_type as enum ('normal', 'terminal');

-- Table org_statuses : statuts personnalisables par organisation
create table public.org_statuses (
  id              uuid primary key default gen_random_uuid(),
  organization_id uuid not null references public.organizations(id) on delete cascade,
  label           text not null,
  type            public.status_type not null default 'normal',
  display_order   integer not null,
  created_at      timestamptz not null default now(),
  -- Contrainte d'unicité : un display_order unique par organisation
  unique (organization_id, display_order)
);

-- Index pour les politiques RLS
create index idx_org_statuses_organization_id on public.org_statuses(organization_id);

comment on table public.org_statuses is 'Statuts personnalisables par organisation — FOUND-04/FOUND-05';
comment on column public.org_statuses.display_order is 'Ordre d''affichage — unique par org, 1-13 pour les statuts pré-chargés';

-- Fonction trigger : pré-charge les 13 statuts par défaut à la création d'une organisation
-- Labels et ordre EXACTS prescrits par CONTEXT.md — ne pas modifier
create or replace function public.seed_default_statuses()
returns trigger
language plpgsql
as $$
begin
  insert into public.org_statuses (organization_id, label, type, display_order)
  values
    -- 7 statuts normaux
    (NEW.id, 'Non commencé',                           'normal',   1),
    (NEW.id, 'Rédaction PV',                            'normal',   2),
    (NEW.id, 'Envoyé au client',                        'normal',   3),
    (NEW.id, 'PV Signé',                                'normal',   4),
    (NEW.id, 'PV / Comptes déposés',                   'normal',   5),
    (NEW.id, 'Attente récépissé',                       'normal',   6),
    (NEW.id, 'Terminé',                                 'normal',   7),
    -- 6 statuts terminaux
    (NEW.id, 'Non déposé (à la demande du client)',     'terminal', 8),
    (NEW.id, 'Fait par avocat',                         'terminal', 9),
    (NEW.id, 'Fait par ancien cabinet',                 'terminal', 10),
    (NEW.id, 'Dépôt non obligatoire (Société civile)',  'terminal', 11),
    (NEW.id, 'Société en liquidation',                  'terminal', 12),
    (NEW.id, 'Absence mission juridique',               'terminal', 13);
  return NEW;
end;
$$;

-- Trigger AFTER INSERT sur organizations — atomique, garantit que chaque org a ses statuts
create trigger trg_seed_org_statuses
after insert on public.organizations
for each row execute function public.seed_default_statuses();
```

Points critiques :
- `user_cabinet_assignments` a une PK COMPOSITE `(user_id, cabinet_id)` — pas une UUID id séparée
- L'enum `status_type` utilise 'normal' et 'terminal' (conformément à CONTEXT.md — pas 'Normal'/'Terminal')
- Le trigger est `AFTER INSERT` (pas BEFORE) — il doit lire `NEW.id` qui est déjà généré
- Les labels des statuts sont EXACTS — accents et caractères spéciaux doivent être préservés mot pour mot
- La contrainte `unique (organization_id, display_order)` empêche les conflits d'ordre
  </action>
  <verify>
    <automated>grep -E "primary key \(user_id, cabinet_id\)" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/20260316000003_create_user_cabinet_assignments.sql && grep -E "after insert on public\.organizations|seed_default_statuses|Non commencé|Absence mission juridique" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/20260316000004_create_org_statuses.sql</automated>
  </verify>
  <done>Migration 003 contient "primary key (user_id, cabinet_id)". Migration 004 contient "after insert on public.organizations", "seed_default_statuses", "Non commencé" (order 1) et "Absence mission juridique" (order 13).</done>
</task>

</tasks>

<verification>
```bash
# Vérifier que les 4 migrations existent
ls /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/

# Vérifier l'absence de colonnes Stripe dans organizations
grep -v "subscription_id\|customer_id\|stripe" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/20260316000001_create_organizations.sql

# Vérifier les 13 statuts dans la fonction trigger
grep -c "NEW.id" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/20260316000004_create_org_statuses.sql
# Doit retourner 13

# Vérifier les enums créés
grep "create type" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/20260316000001_create_organizations.sql
grep "create type" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/20260316000002_create_profiles_cabinets.sql
grep "create type" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/20260316000004_create_org_statuses.sql
```
</verification>

<success_criteria>
- 4 fichiers SQL dans supabase/migrations/ avec les timestamps corrects (20260316000001 à 20260316000004)
- Migration 001 : table organizations avec enum plan_type ('starter', 'pro', 'cabinet', 'enterprise'), active_dossiers_count default 0, SANS colonnes Stripe
- Migration 002 : table profiles avec profiles.id FK vers auth.users(id), organization_id nullable, enum user_role ('expert-comptable', 'collaborateur') + table cabinets avec code nullable
- Migration 003 : table user_cabinet_assignments avec PK composite (user_id, cabinet_id) et index sur user_id
- Migration 004 : table org_statuses + enum status_type ('normal', 'terminal') + fonction seed_default_statuses() + trigger AFTER INSERT sur organizations avec les 13 labels exacts dans l'ordre prescrit
</success_criteria>

<output>
After completion, create `.planning/phases/01-fondation-sch-ma-rls/01-02-SUMMARY.md`
</output>
