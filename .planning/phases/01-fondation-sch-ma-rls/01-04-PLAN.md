---
phase: 01-fondation-sch-ma-rls
plan: 04
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - supabase/migrations/20260316000007_enable_rls_policies.sql
autonomous: true
requirements: [FOUND-02, FOUND-03]

must_haves:
  truths:
    - "RLS est activée sur les 6 tables : organizations, profiles, cabinets, user_cabinet_assignments, org_statuses, dossiers"
    - "Un utilisateur expert-comptable ne voit que les enregistrements de son organisation (organization_id = get_my_org_id())"
    - "Un collaborateur ne voit dans dossiers que les dossiers de ses cabinets assignés (cabinet_id = ANY(get_my_cabinet_ids()))"
    - "Aucun utilisateur authentifié ne peut voir les données d'une autre organisation"
    - "Les politiques RLS utilisent TO authenticated et le wrapping (select private.fn()) pour la performance"
    - "supabase test db passe tous les tests verts (après supabase migration up)"
  artifacts:
    - path: "supabase/migrations/20260316000007_enable_rls_policies.sql"
      provides: "enable row level security sur 6 tables + politiques SELECT/INSERT/UPDATE/DELETE"
      contains: "alter table public.dossiers enable row level security"
  key_links:
    - from: "supabase/migrations/20260316000007_enable_rls_policies.sql"
      to: "supabase/migrations/20260316000006_create_rls_helper_functions.sql"
      via: "Toutes les politiques appellent (select private.get_my_org_id()), (select private.get_my_role()), (select private.get_my_cabinet_ids())"
      pattern: "private\\.get_my_"
    - from: "supabase/tests/02_rls_isolation.test.sql"
      to: "supabase/migrations/20260316000007_enable_rls_policies.sql"
      via: "Les politiques RLS sur dossiers font passer le test is_empty(org_B rows)"
      pattern: "is_empty"
---

<objective>
Activer Row Level Security sur toutes les tables et créer les politiques d'isolation multi-tenant à 3 niveaux (organisation / cabinet / utilisateur). C'est la migration finale de Phase 1 — après elle, aucune donnée d'organisation ne peut fuir vers une autre.

Purpose: Sans RLS activée et sans politiques, les tables sont visibles par tous les utilisateurs authentifiés. Cette migration ferme le multi-tenant au niveau DB — la seule garantie absolue qui ne peut pas être bypassée par un bug applicatif.
Output: Migration 007 avec RLS activée + politiques complètes sur 6 tables. Suite de tests pgTAP verte.
</objective>

<execution_context>
@/Users/zakariabillouche/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zakariabillouche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-fondation-sch-ma-rls/01-CONTEXT.md
@.planning/phases/01-fondation-sch-ma-rls/01-RESEARCH.md
@.planning/phases/01-fondation-sch-ma-rls/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration RLS — activation et politiques sur toutes les tables</name>
  <files>supabase/migrations/20260316000007_enable_rls_policies.sql</files>
  <read_first>
    - .planning/phases/01-fondation-sch-ma-rls/01-RESEARCH.md (§Pattern 2 — Politique RLS avec wrapping initPlan : code SQL complet, §Pattern 4 — Isolation totale entre organisations, §Anti-Patterns : "Policies sans clause TO authenticated", §Common Pitfalls — Pitfall 1 : RLS activée sans politique)
    - .planning/phases/01-fondation-sch-ma-rls/01-CONTEXT.md (§Politiques RLS — décisions locked : expert-comptable voit tout son org, collaborateur limité aux cabinets assignés)
    - supabase/migrations/20260316000006_create_rls_helper_functions.sql (signatures exactes des helpers)
  </read_first>
  <action>
Créer `supabase/migrations/20260316000007_enable_rls_policies.sql` :

```sql
-- Migration 007: Enable RLS and create policies on all tables
-- Phase 1 — Fondation : Schéma & RLS
-- FOUND-02 : isolation multi-tenant 3 niveaux
-- FOUND-03 : JWT custom claims lus via private.get_my_org_id() / private.get_my_role()
--
-- RÈGLE CRITIQUE : activation RLS et politique dans la même migration (Pitfall 1)
-- Une table avec RLS activée mais sans politique retourne 0 lignes silencieusement
--
-- PATTERN PERFORMANCE : toujours (select private.fn()) — jamais private.fn() directement
-- Le wrapping (select ...) force le cache initPlan PostgreSQL (~95% d'amélioration)
--
-- CLAUSE TO : toujours "TO authenticated" — évite évaluation pour anon (99.78% d'amélioration)

-- ============================================================
-- 1. ACTIVER RLS SUR TOUTES LES TABLES
-- ============================================================
alter table public.organizations          enable row level security;
alter table public.profiles               enable row level security;
alter table public.cabinets               enable row level security;
alter table public.user_cabinet_assignments enable row level security;
alter table public.org_statuses           enable row level security;
alter table public.dossiers               enable row level security;

-- ============================================================
-- 2. POLITIQUES : organizations
-- Un utilisateur voit uniquement son organisation
-- ============================================================

create policy "organizations_select"
on public.organizations
for select
to authenticated
using (
  id = (select private.get_my_org_id())
);

create policy "organizations_insert"
on public.organizations
for insert
to authenticated
with check (
  id = (select private.get_my_org_id())
);

create policy "organizations_update"
on public.organizations
for update
to authenticated
using (
  id = (select private.get_my_org_id())
)
with check (
  id = (select private.get_my_org_id())
);

-- Pas de DELETE policy sur organizations — la suppression d'une org est une opération admin

-- ============================================================
-- 3. POLITIQUES : profiles
-- Un utilisateur voit les profils de son organisation
-- ============================================================

create policy "profiles_select"
on public.profiles
for select
to authenticated
using (
  organization_id = (select private.get_my_org_id())
);

create policy "profiles_insert"
on public.profiles
for insert
to authenticated
with check (
  id = (select auth.uid())
);

create policy "profiles_update"
on public.profiles
for update
to authenticated
using (
  id = (select auth.uid())
)
with check (
  id = (select auth.uid())
);

-- ============================================================
-- 4. POLITIQUES : cabinets
-- Un utilisateur voit les cabinets de son organisation
-- Expert-comptable peut créer/modifier, collaborateur lecture seule
-- ============================================================

create policy "cabinets_select"
on public.cabinets
for select
to authenticated
using (
  organization_id = (select private.get_my_org_id())
);

create policy "cabinets_insert"
on public.cabinets
for insert
to authenticated
with check (
  organization_id = (select private.get_my_org_id())
  and (select private.get_my_role()) = 'expert-comptable'
);

create policy "cabinets_update"
on public.cabinets
for update
to authenticated
using (
  organization_id = (select private.get_my_org_id())
  and (select private.get_my_role()) = 'expert-comptable'
)
with check (
  organization_id = (select private.get_my_org_id())
);

-- ============================================================
-- 5. POLITIQUES : user_cabinet_assignments
-- Un utilisateur voit ses propres assignations
-- Expert-comptable peut gérer les assignations de son org
-- ============================================================

create policy "user_cabinet_assignments_select"
on public.user_cabinet_assignments
for select
to authenticated
using (
  cabinet_id in (
    select id from public.cabinets
    where organization_id = (select private.get_my_org_id())
  )
);

create policy "user_cabinet_assignments_insert"
on public.user_cabinet_assignments
for insert
to authenticated
with check (
  cabinet_id in (
    select id from public.cabinets
    where organization_id = (select private.get_my_org_id())
  )
  and (select private.get_my_role()) = 'expert-comptable'
);

create policy "user_cabinet_assignments_delete"
on public.user_cabinet_assignments
for delete
to authenticated
using (
  cabinet_id in (
    select id from public.cabinets
    where organization_id = (select private.get_my_org_id())
  )
  and (select private.get_my_role()) = 'expert-comptable'
);

-- ============================================================
-- 6. POLITIQUES : org_statuses
-- Tous les membres voient les statuts de leur organisation
-- Expert-comptable peut créer/modifier/supprimer (si pas utilisé — bloqué par FK RESTRICT)
-- ============================================================

create policy "org_statuses_select"
on public.org_statuses
for select
to authenticated
using (
  organization_id = (select private.get_my_org_id())
);

create policy "org_statuses_insert"
on public.org_statuses
for insert
to authenticated
with check (
  organization_id = (select private.get_my_org_id())
  and (select private.get_my_role()) = 'expert-comptable'
);

create policy "org_statuses_update"
on public.org_statuses
for update
to authenticated
using (
  organization_id = (select private.get_my_org_id())
  and (select private.get_my_role()) = 'expert-comptable'
)
with check (
  organization_id = (select private.get_my_org_id())
);

create policy "org_statuses_delete"
on public.org_statuses
for delete
to authenticated
using (
  organization_id = (select private.get_my_org_id())
  and (select private.get_my_role()) = 'expert-comptable'
);
-- Note : la FK ON DELETE RESTRICT sur dossiers.status_id bloque la suppression si le statut est utilisé

-- ============================================================
-- 7. POLITIQUES : dossiers — TABLE CENTRALE
-- Expert-comptable : voit TOUS les dossiers de son org
-- Collaborateur : voit UNIQUEMENT les dossiers de ses cabinets assignés
-- ============================================================

create policy "dossiers_select_expert_comptable"
on public.dossiers
for select
to authenticated
using (
  (select private.get_my_role()) = 'expert-comptable'
  and organization_id = (select private.get_my_org_id())
);

create policy "dossiers_select_collaborateur"
on public.dossiers
for select
to authenticated
using (
  (select private.get_my_role()) = 'collaborateur'
  and cabinet_id = any(select private.get_my_cabinet_ids())
);

create policy "dossiers_insert"
on public.dossiers
for insert
to authenticated
with check (
  organization_id = (select private.get_my_org_id())
);

create policy "dossiers_update"
on public.dossiers
for update
to authenticated
using (
  organization_id = (select private.get_my_org_id())
)
with check (
  organization_id = (select private.get_my_org_id())
);

-- Pas de DELETE policy sur dossiers — suppression = opération admin ou via CASCADE uniquement
```

Points critiques :
- `alter table ... enable row level security` et les `create policy` sont dans la MÊME migration (Pitfall 1 : pas de migration d'activation sans politiques)
- Chaque politique a `TO authenticated` (pas `TO public`)
- Chaque appel de fonction helper utilise `(select private.fn())` — pas `private.fn()` directement
- Les 2 politiques SELECT sur `dossiers` sont SÉPARÉES par rôle — elles ne s'excluent pas mutuellement dans PostgreSQL (OR implicite entre policies du même type)
- Il n'y a PAS de politique DELETE sur `dossiers` ni sur `organizations` — opérations admin seulement
  </action>
  <verify>
    <automated>grep -c "enable row level security" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/20260316000007_enable_rls_policies.sql && grep -c "create policy" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/20260316000007_enable_rls_policies.sql</automated>
  </verify>
  <done>Le fichier contient exactement 6 occurrences de "enable row level security" (une par table) et au moins 15 politiques "create policy". Chaque politique utilise "TO authenticated" et "(select private.get_my_".</done>
</task>

<task type="auto">
  <name>Task 2: Appliquer les migrations et valider les tests pgTAP</name>
  <files>supabase/migrations/ (read-only — aucune modification)</files>
  <read_first>
    - .planning/phases/01-fondation-sch-ma-rls/01-RESEARCH.md (§Standard Stack — commandes CLI : supabase start, migration up, test db)
    - .planning/phases/01-fondation-sch-ma-rls/01-VALIDATION.md (§Sampling Rate)
  </read_first>
  <action>
Depuis la racine du projet `/Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal` :

1. Vérifier que Supabase local est démarré — prérequis obligatoire :
```bash
supabase status
```
Si la commande retourne une erreur de connexion ou "not running", démarrer d'abord :
```bash
supabase start
```
Attendre que `supabase start` se termine complètement avant de continuer. Cette étape peut prendre 1-2 minutes au premier démarrage (téléchargement des images Docker).

2. Appliquer toutes les migrations (001 → 007) :
```bash
supabase db reset
```
(db reset repart de zéro et applique toutes les migrations dans l'ordre — préférable à `migration up` pour garantir l'état propre)

3. Lancer la suite de tests pgTAP :
```bash
supabase test db
```

4. Si des tests échouent, diagnostiquer :
   - Erreur "relation does not exist" → problème d'ordre de migration (vérifier les timestamps)
   - Tests RLS retournent des données d'une autre org → politique manquante ou incorrecte
   - Test trigger retourne ≠ 13 → bug dans la fonction seed_default_statuses()
   - Test throws_ok échoue → FK status_id n'est pas ON DELETE RESTRICT

5. Corriger les migrations défaillantes et relancer `supabase db reset && supabase test db` jusqu'à ce que tous les tests soient verts.

**Critère de succès de cette tâche : `supabase test db` termine avec 0 erreurs.**

Note : si pgTAP n'est pas installé dans la DB locale, ajouter dans 00_setup.sql :
```sql
create extension if not exists pgtap;
```
Et relancer `supabase db reset`.
  </action>
  <verify>
    <automated>cd /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal && supabase status 2>&1 | grep -q "DB URL" && supabase test db 2>&1 | tail -5</automated>
  </verify>
  <done>Prérequis : `supabase start` doit avoir été exécuté avec succès avant cette tâche — `supabase status` doit afficher "DB URL" sans erreur de connexion. Une fois l'infrastructure confirmée active, `supabase test db` se termine sans ligne "FAILED" — tous les tests pgTAP passent en vert. Le output final affiche "ok" ou "# Looks like everything went fine."</done>
</task>

</tasks>

<verification>
```bash
# Vérifier RLS activée sur 6 tables
grep -c "enable row level security" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/20260316000007_enable_rls_policies.sql
# Doit retourner 6

# Vérifier les 2 politiques SELECT séparées sur dossiers
grep "dossiers_select" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/20260316000007_enable_rls_policies.sql
# Doit afficher : dossiers_select_expert_comptable et dossiers_select_collaborateur

# Vérifier le pattern performance dans toutes les politiques
grep "select private\.get_my_" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/migrations/20260316000007_enable_rls_policies.sql | wc -l
# Doit retourner > 10

# Vérifier que Supabase est démarré avant de lancer les tests
supabase status && supabase test db
```
</verification>

<success_criteria>
- Migration 007 contient 6 "enable row level security" (une par table)
- Migration 007 contient au minimum : organizations (3 policies), profiles (3), cabinets (3), user_cabinet_assignments (3), org_statuses (4), dossiers (4) = 20 policies minimum
- Toutes les politiques ont "TO authenticated" et "(select private.get_my_"
- `supabase status` confirme que l'infrastructure est active avant `supabase test db`
- "supabase test db" passe en vert — tous les requirements FOUND-01 à FOUND-05 sont vérifiés automatiquement
- Critères de succès Phase 1 (ROADMAP.md) vérifiables : SELECT sur dossiers depuis org_A retourne 0 lignes de org_B, collaborateur limité à ses cabinets, DELETE d'un statut utilisé lève 23503
</success_criteria>

<output>
After completion, create `.planning/phases/01-fondation-sch-ma-rls/01-04-SUMMARY.md`
</output>
