---
phase: 01-fondation-sch-ma-rls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/tests/00_setup.sql
  - supabase/tests/01_schema.test.sql
  - supabase/tests/02_rls_isolation.test.sql
  - supabase/tests/03_rls_collaborateur.test.sql
  - supabase/tests/04_status_seed.test.sql
  - supabase/tests/05_status_restrict.test.sql
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03, FOUND-04, FOUND-05]

must_haves:
  truths:
    - "supabase test db retourne 0 tests failed (ou les tests sont des stubs SKIP/TODO explicites)"
    - "Les tests existent dans supabase/tests/ et couvrent chaque requirement ID (FOUND-01 à FOUND-05)"
    - "supabase init a été exécuté — le répertoire supabase/ existe à la racine du projet"
  artifacts:
    - path: "supabase/tests/00_setup.sql"
      provides: "Extension pgTAP + fixtures communes (2 orgs, 2 users, 2 cabinets, 1 statut)"
    - path: "supabase/tests/01_schema.test.sql"
      provides: "Tests de structure de schéma (colonnes nullables, contraintes)"
      contains: "FOUND-01"
    - path: "supabase/tests/02_rls_isolation.test.sql"
      provides: "Tests d'isolation org_A vs org_B"
      contains: "FOUND-02"
    - path: "supabase/tests/03_rls_collaborateur.test.sql"
      provides: "Tests de restriction collaborateur aux cabinets assignés"
      contains: "FOUND-02 FOUND-03"
    - path: "supabase/tests/04_status_seed.test.sql"
      provides: "Tests du trigger de pré-chargement des 13 statuts"
      contains: "FOUND-04"
    - path: "supabase/tests/05_status_restrict.test.sql"
      provides: "Tests ON DELETE RESTRICT sur dossiers.status_id"
      contains: "FOUND-05"
  key_links:
    - from: "supabase/tests/00_setup.sql"
      to: "supabase/tests/*.test.sql"
      via: "BEGIN/ROLLBACK wrapping — chaque test file inclut le teardown"
      pattern: "rollback"
---

<objective>
Initialiser le projet Supabase et créer les scaffolds de tests pgTAP pour tous les requirements de Phase 1. Ces tests échouent d'abord (RED) — c'est attendu. Les migrations suivantes (Plans 02-04) les feront passer (GREEN).

Purpose: Appliquer le principe Nyquist — les tests doivent exister avant l'implémentation pour garantir que chaque requirement est vérifiable de façon automatisée.
Output: Répertoire supabase/ initialisé + 6 fichiers de tests pgTAP couvrant FOUND-01 à FOUND-05.
</objective>

<execution_context>
@/Users/zakariabillouche/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zakariabillouche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-fondation-sch-ma-rls/01-CONTEXT.md
@.planning/phases/01-fondation-sch-ma-rls/01-RESEARCH.md
@.planning/phases/01-fondation-sch-ma-rls/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialiser Supabase CLI et créer le répertoire de tests</name>
  <files>supabase/config.toml, supabase/tests/</files>
  <read_first>
    - .planning/phases/01-fondation-sch-ma-rls/01-RESEARCH.md (§Standard Stack — commandes CLI, §Architecture Patterns — structure recommandée)
    - .planning/phases/01-fondation-sch-ma-rls/01-CONTEXT.md (§Outil de migrations — décision locked)
  </read_first>
  <action>
Depuis la racine du projet `/Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal` :

1. Exécuter `supabase init` pour créer le répertoire `supabase/` avec `config.toml`. Si supabase/ existe déjà, vérifier que config.toml est présent.

2. Créer manuellement le répertoire `supabase/tests/` s'il n'existe pas.

3. Créer `supabase/tests/00_setup.sql` avec le contenu suivant (fixtures communes utilisées par tous les tests — wrappées dans BEGIN/ROLLBACK pour isolation) :

```sql
-- 00_setup.sql — Fixtures communes Phase 1
-- Installe pgTAP et prépare les données de test
-- À inclure via \i ou comme premier fichier dans supabase test db

create extension if not exists pgtap;

-- Les fixtures sont créées dans chaque test file avec BEGIN/ROLLBACK
-- Ce fichier sert uniquement à activer l'extension pgTAP
```

Note : `supabase start` n'est PAS requis maintenant — le but est uniquement de créer les fichiers. Les tests seront exécutés dans les plans suivants.
  </action>
  <verify>
    <automated>ls /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/config.toml && ls /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/tests/00_setup.sql</automated>
  </verify>
  <done>supabase/config.toml existe. supabase/tests/00_setup.sql existe et contient "create extension if not exists pgtap".</done>
</task>

<task type="auto">
  <name>Task 2: Créer les tests de schéma (FOUND-01, FOUND-05)</name>
  <files>supabase/tests/01_schema.test.sql, supabase/tests/05_status_restrict.test.sql</files>
  <read_first>
    - .planning/phases/01-fondation-sch-ma-rls/01-RESEARCH.md (§Code Examples — "Test pgTAP — vérifier ON DELETE RESTRICT", §Phase Requirements — FOUND-01 et FOUND-05)
    - .planning/phases/01-fondation-sch-ma-rls/01-CONTEXT.md (§Structure des tables — colonnes exactes de dossiers, §Champs AGO-spécifiques)
    - .planning/phases/01-fondation-sch-ma-rls/01-VALIDATION.md (§Wave 0 Requirements)
  </read_first>
  <action>
Créer `supabase/tests/01_schema.test.sql` :

```sql
-- 01_schema.test.sql — Tests de structure du schéma
-- Couvre : FOUND-01 (champs AGO nullables), FOUND-05 (ON DELETE RESTRICT via colonnes)
begin;
select plan(8);

-- FOUND-01 : la table dossiers existe avec les bonnes colonnes
select has_table('public', 'dossiers', 'La table dossiers doit exister');
select has_column('public', 'dossiers', 'type', 'dossiers doit avoir un champ type');
select has_column('public', 'dossiers', 'task_uid', 'dossiers doit avoir un champ task_uid');
select col_is_null('public', 'dossiers', 'date_cloture', 'date_cloture doit être nullable (champ AGO-spécifique)');
select col_is_null('public', 'dossiers', 'date_echeance', 'date_echeance doit être nullable (champ AGO-spécifique)');
select col_is_null('public', 'dossiers', 'forme_juridique', 'forme_juridique doit être nullable (champ AGO-spécifique)');
select col_is_null('public', 'dossiers', 'siren', 'siren doit être nullable (champ AGO-spécifique)');

-- FOUND-05 : la FK status_id référence org_statuses
select col_is_fk('public', 'dossiers', 'status_id', 'dossiers.status_id doit être une FK vers org_statuses');

select * from finish();
rollback;
```

Créer `supabase/tests/05_status_restrict.test.sql` :

```sql
-- 05_status_restrict.test.sql — Test ON DELETE RESTRICT (FOUND-05)
begin;
select plan(1);

-- Setup : créer une org, un statut, un dossier qui référence ce statut
-- puis tenter de supprimer le statut — doit lever une erreur FK 23503

do $$
declare
  v_org_id uuid;
  v_status_id uuid;
  v_dossier_id uuid;
begin
  -- Insérer une organisation de test
  insert into public.organizations (name, plan)
  values ('Test Org Restrict', 'starter')
  returning id into v_org_id;

  -- Récupérer un statut créé par le trigger
  select id into v_status_id
  from public.org_statuses
  where organization_id = v_org_id
  limit 1;

  -- Insérer un dossier qui référence ce statut
  insert into public.dossiers (organization_id, cabinet_id, type, status_id)
  select v_org_id, id, 'AGO', v_status_id
  from public.cabinets
  where organization_id = v_org_id
  limit 1;

  v_dossier_id := lastval()::uuid;
end $$;

-- Tenter de supprimer un statut utilisé — doit lever 23503
select throws_ok(
  $$ delete from public.org_statuses
     where id = (
       select status_id from public.dossiers limit 1
     ) $$,
  '23503',
  null,
  'La suppression d''un statut référencé par un dossier doit lever foreign_key_violation (23503)'
);

select * from finish();
rollback;
```
  </action>
  <verify>
    <automated>grep -l "FOUND-01\|col_is_null.*date_cloture" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/tests/01_schema.test.sql && grep "23503" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/tests/05_status_restrict.test.sql</automated>
  </verify>
  <done>supabase/tests/01_schema.test.sql contient "col_is_null" pour date_cloture et "col_is_fk" pour status_id. supabase/tests/05_status_restrict.test.sql contient "23503" et "throws_ok".</done>
</task>

<task type="auto">
  <name>Task 3: Créer les tests RLS et trigger de statuts (FOUND-02, FOUND-03, FOUND-04)</name>
  <files>supabase/tests/02_rls_isolation.test.sql, supabase/tests/03_rls_collaborateur.test.sql, supabase/tests/04_status_seed.test.sql</files>
  <read_first>
    - .planning/phases/01-fondation-sch-ma-rls/01-RESEARCH.md (§Code Examples — "Test pgTAP — vérifier l'isolation multi-tenant", §Pattern 3 — Trigger de pré-chargement, §Common Pitfalls — Pitfall 2 tests via SQL Editor)
    - .planning/phases/01-fondation-sch-ma-rls/01-CONTEXT.md (§Statuts pré-chargés — labels et ordre exact des 13 statuts, §Politiques RLS)
  </read_first>
  <action>
Créer `supabase/tests/02_rls_isolation.test.sql` (FOUND-02 — isolation org_A vs org_B) :

```sql
-- 02_rls_isolation.test.sql — Tests d'isolation multi-tenant org_A vs org_B
-- Couvre : FOUND-02, FOUND-03
begin;
select plan(2);

-- Simuler le contexte d'un utilisateur de org_A avec JWT custom claims
set local role authenticated;
set local request.jwt.claims = '{"sub": "00000000-0000-0000-0000-000000000001", "app_metadata": {"org_id": "00000000-0000-0000-0000-000000000010", "role": "expert-comptable"}}';

-- Un utilisateur de org_A ne doit voir aucune ligne appartenant à org_B
select is_empty(
  $$ select * from public.dossiers
     where organization_id = '00000000-0000-0000-0000-000000000020' $$,
  'FOUND-02 : un utilisateur org_A ne doit voir aucune ligne de org_B'
);

-- Un utilisateur de org_A doit voir ses propres dossiers
select isnt_empty(
  $$ select * from public.dossiers
     where organization_id = '00000000-0000-0000-0000-000000000010' $$,
  'FOUND-02 : un utilisateur org_A doit voir ses propres dossiers'
);

select * from finish();
rollback;
```

Créer `supabase/tests/03_rls_collaborateur.test.sql` (FOUND-02/FOUND-03 — restriction collaborateur) :

```sql
-- 03_rls_collaborateur.test.sql — Tests restriction collaborateur aux cabinets assignés
-- Couvre : FOUND-02 (niveau utilisateur), FOUND-03 (JWT claims lus dans RLS)
begin;
select plan(2);

-- Simuler un collaborateur avec accès uniquement au cabinet_A (pas cabinet_B)
set local role authenticated;
set local request.jwt.claims = '{"sub": "00000000-0000-0000-0000-000000000002", "app_metadata": {"org_id": "00000000-0000-0000-0000-000000000010", "role": "collaborateur"}}';

-- Le collaborateur ne doit pas voir les dossiers d'un cabinet non assigné
select is_empty(
  $$ select * from public.dossiers
     where cabinet_id = '00000000-0000-0000-0000-000000000031' $$,
  'FOUND-02/03 : collaborateur ne doit pas voir les dossiers d''un cabinet non assigné'
);

-- Le collaborateur doit voir les dossiers de son cabinet assigné
select isnt_empty(
  $$ select * from public.dossiers
     where cabinet_id = '00000000-0000-0000-0000-000000000030' $$,
  'FOUND-02/03 : collaborateur doit voir les dossiers de ses cabinets assignés'
);

select * from finish();
rollback;
```

Créer `supabase/tests/04_status_seed.test.sql` (FOUND-04 — trigger de pré-chargement) :

```sql
-- 04_status_seed.test.sql — Tests du trigger de pré-chargement des 13 statuts
-- Couvre : FOUND-04
--
-- NOTE SYNTAXE : pgTAP s'exécute en contexte SQL pur — DECLARE et RETURNING...INTO
-- ne sont valides qu'à l'intérieur d'un bloc DO $$. Pour éviter cette erreur, toutes
-- les assertions utilisent un sous-SELECT sur le nom de l'organisation au lieu d'une
-- variable locale.
begin;
select plan(16);

-- Insérer une nouvelle organisation — le trigger doit pré-charger 13 statuts
insert into public.organizations (name, plan)
values ('Test Org Statuses', 'starter');

-- Vérifier le compte total : exactement 13 statuts
select is(
  (select count(*)::int from public.org_statuses
   where organization_id = (select id from public.organizations where name = 'Test Org Statuses')),
  13,
  'FOUND-04 : le trigger doit créer exactement 13 statuts par organisation'
);

-- Vérifier le compte des statuts normaux : 7
select is(
  (select count(*)::int from public.org_statuses
   where organization_id = (select id from public.organizations where name = 'Test Org Statuses')
     and type = 'normal'),
  7,
  'FOUND-04 : 7 statuts normaux doivent être créés'
);

-- Vérifier le compte des statuts terminaux : 6
select is(
  (select count(*)::int from public.org_statuses
   where organization_id = (select id from public.organizations where name = 'Test Org Statuses')
     and type = 'terminal'),
  6,
  'FOUND-04 : 6 statuts terminaux doivent être créés'
);

-- Vérifier les labels exacts et display_order des 7 statuts normaux
select is(
  (select label from public.org_statuses
   where organization_id = (select id from public.organizations where name = 'Test Org Statuses')
     and display_order = 1),
  'Non commencé',
  'FOUND-04 : statut order 1 = Non commencé'
);
select is(
  (select label from public.org_statuses
   where organization_id = (select id from public.organizations where name = 'Test Org Statuses')
     and display_order = 2),
  'Rédaction PV',
  'FOUND-04 : statut order 2 = Rédaction PV'
);
select is(
  (select label from public.org_statuses
   where organization_id = (select id from public.organizations where name = 'Test Org Statuses')
     and display_order = 3),
  'Envoyé au client',
  'FOUND-04 : statut order 3 = Envoyé au client'
);
select is(
  (select label from public.org_statuses
   where organization_id = (select id from public.organizations where name = 'Test Org Statuses')
     and display_order = 4),
  'PV Signé',
  'FOUND-04 : statut order 4 = PV Signé'
);
select is(
  (select label from public.org_statuses
   where organization_id = (select id from public.organizations where name = 'Test Org Statuses')
     and display_order = 5),
  'PV / Comptes déposés',
  'FOUND-04 : statut order 5 = PV / Comptes déposés'
);
select is(
  (select label from public.org_statuses
   where organization_id = (select id from public.organizations where name = 'Test Org Statuses')
     and display_order = 6),
  'Attente récépissé',
  'FOUND-04 : statut order 6 = Attente récépissé'
);
select is(
  (select label from public.org_statuses
   where organization_id = (select id from public.organizations where name = 'Test Org Statuses')
     and display_order = 7),
  'Terminé',
  'FOUND-04 : statut order 7 = Terminé (dernier normal)'
);

-- Vérifier les labels exacts des 6 statuts terminaux
select is(
  (select label from public.org_statuses
   where organization_id = (select id from public.organizations where name = 'Test Org Statuses')
     and display_order = 8),
  'Non déposé (à la demande du client)',
  'FOUND-04 : statut order 8 = Non déposé (à la demande du client)'
);
select is(
  (select label from public.org_statuses
   where organization_id = (select id from public.organizations where name = 'Test Org Statuses')
     and display_order = 9),
  'Fait par avocat',
  'FOUND-04 : statut order 9 = Fait par avocat'
);
select is(
  (select label from public.org_statuses
   where organization_id = (select id from public.organizations where name = 'Test Org Statuses')
     and display_order = 10),
  'Fait par ancien cabinet',
  'FOUND-04 : statut order 10 = Fait par ancien cabinet'
);
select is(
  (select label from public.org_statuses
   where organization_id = (select id from public.organizations where name = 'Test Org Statuses')
     and display_order = 11),
  'Dépôt non obligatoire (Société civile)',
  'FOUND-04 : statut order 11 = Dépôt non obligatoire (Société civile)'
);
select is(
  (select label from public.org_statuses
   where organization_id = (select id from public.organizations where name = 'Test Org Statuses')
     and display_order = 12),
  'Société en liquidation',
  'FOUND-04 : statut order 12 = Société en liquidation'
);
select is(
  (select label from public.org_statuses
   where organization_id = (select id from public.organizations where name = 'Test Org Statuses')
     and display_order = 13),
  'Absence mission juridique',
  'FOUND-04 : statut order 13 = Absence mission juridique'
);

select * from finish();
rollback;
```

Note sur la syntaxe : pgTAP tourne dans un contexte SQL — les variables locales (`declare`) ne sont pas disponibles hors d'un bloc `DO $$`. Pour les tests 02 et 03, les fixtures avec les UUIDs fixes seront créées en postgres avant `set local role authenticated` (postgres bypass RLS). Les tests 02/03 supposent que des données de test avec ces UUIDs existent dans la DB (configuré dans 00_setup.sql ou dans chaque test avant le `set local role`).

Ajouter au début de `02_rls_isolation.test.sql` et `03_rls_collaborateur.test.sql`, avant `set local role authenticated`, le bloc de setup suivant :

```sql
-- Setup fixtures en mode postgres (bypass RLS)
-- Org A
insert into public.organizations (id, name, plan)
values ('00000000-0000-0000-0000-000000000010', 'Cabinet A', 'starter')
on conflict (id) do nothing;

-- Org B
insert into public.organizations (id, name, plan)
values ('00000000-0000-0000-0000-000000000020', 'Cabinet B', 'starter')
on conflict (id) do nothing;

-- ... (cabinets, dossiers, user_cabinet_assignments similaires)
```

Ces insertions seront dans BEGIN/ROLLBACK donc ne polluent pas la DB.
  </action>
  <verify>
    <automated>grep -l "FOUND-04\|status_seed\|13 statuts" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/tests/04_status_seed.test.sql && grep "FOUND-02" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/tests/02_rls_isolation.test.sql && grep "collaborateur" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/tests/03_rls_collaborateur.test.sql</automated>
  </verify>
  <done>supabase/tests/02_rls_isolation.test.sql contient "FOUND-02" et "is_empty". supabase/tests/03_rls_collaborateur.test.sql contient "collaborateur". supabase/tests/04_status_seed.test.sql contient exactement 13 assertions sur les labels et display_order des statuts, toutes utilisant le pattern subquery `(select id from public.organizations where name = 'Test Org Statuses')` — aucune variable DECLARE hors d'un DO block.</done>
</task>

</tasks>

<verification>
```bash
# Vérifier que tous les fichiers de tests existent
ls /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/supabase/tests/

# Vérifier que chaque requirement est couvert
grep -r "FOUND-01\|col_is_null" supabase/tests/01_schema.test.sql
grep -r "FOUND-02\|is_empty" supabase/tests/02_rls_isolation.test.sql
grep -r "FOUND-02\|collaborateur" supabase/tests/03_rls_collaborateur.test.sql
grep -r "FOUND-04\|13 statuts" supabase/tests/04_status_seed.test.sql
grep -r "23503\|throws_ok" supabase/tests/05_status_restrict.test.sql

# Vérifier l'absence de DECLARE hors DO block dans 04_status_seed.test.sql
grep -n "^declare\|^DECLARE" supabase/tests/04_status_seed.test.sql
# Doit retourner vide — aucune ligne
```
</verification>

<success_criteria>
- supabase/config.toml existe (supabase init a été exécuté)
- 6 fichiers de tests pgTAP existent dans supabase/tests/
- Chaque fichier de test est wrappé dans BEGIN/ROLLBACK
- FOUND-01 à FOUND-05 sont chacun couverts par au moins un fichier de test
- Les tests contiennent les assertions pgTAP correctes (is_empty, isnt_empty, throws_ok, col_is_null, col_is_fk, is)
- Les labels exacts des 13 statuts sont vérifiés dans 04_status_seed.test.sql
- 04_status_seed.test.sql n'utilise aucun DECLARE / RETURNING...INTO hors d'un DO block — toutes les assertions utilisent le pattern subquery sur le nom de l'organisation
</success_criteria>

<output>
After completion, create `.planning/phases/01-fondation-sch-ma-rls/01-01-SUMMARY.md`
</output>
