---
phase: 03-dashboard-table-filtres-kpi
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-07
  - DASH-08

must_haves:
  truths:
    - "bun run build exits 0 before any Phase 3 components are added"
    - "react-hot-toast, @tanstack/react-table, and nuqs are in package.json dependencies"
    - "shadcn card, badge, sheet, select, tooltip, popover components exist in src/components/ui/"
    - "src/app/layout.tsx wraps children with NuqsAdapter and renders <Toaster /> from react-hot-toast"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout with NuqsAdapter and Toaster providers"
      contains: "NuqsAdapter"
    - path: "src/components/ui/card.tsx"
      provides: "shadcn Card component"
    - path: "src/components/ui/badge.tsx"
      provides: "shadcn Badge component"
    - path: "src/components/ui/sheet.tsx"
      provides: "shadcn Sheet component"
    - path: "src/components/ui/select.tsx"
      provides: "shadcn Select component"
    - path: "src/components/ui/tooltip.tsx"
      provides: "shadcn Tooltip component"
  key_links:
    - from: "src/app/layout.tsx"
      to: "NuqsAdapter from nuqs/adapters/next/app"
      via: "import and JSX wrapper around {children}"
      pattern: "NuqsAdapter"
    - from: "src/app/layout.tsx"
      to: "Toaster from react-hot-toast"
      via: "import and rendered inside <body>"
      pattern: "Toaster"
---

<objective>
Install all missing packages and shadcn components required by Phase 3. Patch the root layout to add NuqsAdapter (required for URL-persistent filters) and the react-hot-toast Toaster provider. Establish a clean baseline build.

Purpose: All downstream plans (02, 03, 04) import from these packages and providers. This plan must succeed first so subsequent waves never encounter missing module errors.
Output: Working build baseline, three new npm packages, six new shadcn components, patched layout.tsx.
</objective>

<execution_context>
@/Users/zakariabillouche/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zakariabillouche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-table-filtres-kpi/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install npm packages and shadcn components</name>
  <files>package.json, src/components/ui/card.tsx, src/components/ui/badge.tsx, src/components/ui/sheet.tsx, src/components/ui/select.tsx, src/components/ui/tooltip.tsx, src/components/ui/popover.tsx</files>

  <read_first>
    - src/components/ui/avatar.tsx — to verify current ui component inventory
    - src/components/ui/button.tsx — to verify ui component inventory
    - package.json — to verify current dependencies before installing
  </read_first>

  <action>
Run these commands in order. NEVER use npm or npx — only bun/bunx.

Step 1 — Verify baseline build passes before touching anything:
```
bunx tsc --noEmit
bun run build
```
If build fails, note the errors but continue (they may be in the existing stub dashboard page which will be replaced in Plan 03).

Step 2 — Install npm packages:
```
bun add @tanstack/react-table react-hot-toast nuqs
```

Step 3 — Check which shadcn components already exist (DO NOT reinstall existing ones):
```
ls src/components/ui/
```
Expected missing (install these): card, badge, sheet, select, tooltip, popover
Expected present (skip): avatar, button, form, input, label, separator, sonner

Step 4 — Install missing shadcn components one by one using bunx (not npx):
```
bunx shadcn@latest add card
bunx shadcn@latest add badge
bunx shadcn@latest add sheet
bunx shadcn@latest add select
bunx shadcn@latest add tooltip
bunx shadcn@latest add popover
```
If prompted about overwriting existing files, answer NO (preserve existing components).

Do NOT install: framer-motion (already at v12.37.0), motion (already installed), lucide-react (already installed).
  </action>

  <verify>
    <automated>grep -l "@tanstack/react-table\|react-hot-toast\|nuqs" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/package.json && ls /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/src/components/ui/card.tsx /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/src/components/ui/badge.tsx /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/src/components/ui/sheet.tsx /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/src/components/ui/tooltip.tsx && echo "OK"</automated>
  </verify>

  <acceptance_criteria>
    - package.json contains "@tanstack/react-table" in dependencies
    - package.json contains "react-hot-toast" in dependencies
    - package.json contains "nuqs" in dependencies
    - src/components/ui/card.tsx exists
    - src/components/ui/badge.tsx exists
    - src/components/ui/sheet.tsx exists
    - src/components/ui/select.tsx exists
    - src/components/ui/tooltip.tsx exists
    - src/components/ui/popover.tsx exists
    - src/components/ui/avatar.tsx still exists (not overwritten)
    - src/components/ui/button.tsx still exists (not overwritten)
  </acceptance_criteria>

  <done>All required packages installed. All six shadcn components installed. Existing components untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Patch root layout — add NuqsAdapter and Toaster</name>
  <files>src/app/layout.tsx</files>

  <read_first>
    - src/app/layout.tsx — MUST read current content before modifying (file uses Geist font, suppressHydrationWarning on html)
  </read_first>

  <action>
Read the current src/app/layout.tsx first (it has Geist font setup and suppressHydrationWarning).

Replace the file content with a version that:
1. Keeps all existing imports (Geist font, Metadata, globals.css)
2. Adds two new imports:
   - `import { NuqsAdapter } from 'nuqs/adapters/next/app'`
   - `import { Toaster } from 'react-hot-toast'`
3. Wraps `{children}` with `<NuqsAdapter>` inside `<body>`
4. Renders `<Toaster position="top-right" toastOptions={{ duration: 4000 }} />` inside `<body>` after `</NuqsAdapter>`
5. Preserves `suppressHydrationWarning` on `<html>`
6. Preserves `lang="fr"` on `<html>`
7. Preserves `className={geistSans.variable}` on `<html>`
8. Preserves `className="antialiased"` on `<body>`

Final structure of `<body>` content:
```tsx
<body className="antialiased">
  <NuqsAdapter>
    {children}
  </NuqsAdapter>
  <Toaster position="top-right" toastOptions={{ duration: 4000 }} />
</body>
```

Do NOT add any other providers or imports. Do NOT modify the `<html>` element attributes.
  </action>

  <verify>
    <automated>grep -c "NuqsAdapter\|react-hot-toast\|Toaster" /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal/src/app/layout.tsx</automated>
  </verify>

  <acceptance_criteria>
    - src/app/layout.tsx contains `NuqsAdapter` (import and JSX usage)
    - src/app/layout.tsx contains `from 'nuqs/adapters/next/app'`
    - src/app/layout.tsx contains `Toaster` (import and JSX usage)
    - src/app/layout.tsx contains `from 'react-hot-toast'`
    - src/app/layout.tsx still contains `suppressHydrationWarning`
    - src/app/layout.tsx still contains `lang="fr"`
    - src/app/layout.tsx still contains `geistSans.variable`
    - `bunx tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>Root layout wraps all pages with NuqsAdapter (required for nuqs URL state) and renders Toaster (required for react-hot-toast). TypeScript check passes.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `bunx tsc --noEmit` must exit 0
2. `bun run build` should exit 0 (existing stub dashboard page may cause build errors — that is acceptable if the errors are only in `src/app/dashboard/page.tsx` which will be replaced in Plan 03)
3. `ls src/components/ui/` shows: avatar.tsx, badge.tsx, button.tsx, card.tsx, form.tsx, input.tsx, label.tsx, popover.tsx, select.tsx, separator.tsx, sheet.tsx, sonner.tsx, tooltip.tsx
4. `grep "NuqsAdapter\|Toaster" src/app/layout.tsx` returns 4+ matches
</verification>

<success_criteria>
- All 3 npm packages installed and in package.json
- All 6 shadcn components exist in src/components/ui/
- src/app/layout.tsx has NuqsAdapter wrapping children and Toaster rendered
- TypeScript check passes (`bunx tsc --noEmit` exits 0)
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-table-filtres-kpi/03-01-SUMMARY.md`
</output>
