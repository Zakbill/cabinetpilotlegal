---
phase: 02-authentification-rbac
plan: 08
type: execute
wave: 5
depends_on: [02-07]
files_modified: []
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, RBAC-01, RBAC-02, RBAC-03]

must_haves:
  truths:
    - "L'utilisateur peut envoyer un magic link depuis /login et le recevoir par email"
    - "Après clic sur le magic link, le callback redirige vers /onboarding si profil incomplet"
    - "Le wizard onboarding complète les 4 étapes avec transitions animées"
    - "Après complétion, /dashboard est accessible et affiche prénom/nom/rôle"
    - "Une route protégée sans session redirige vers /login"
    - "Un profil incomplet sur n'importe quelle route redirige vers /onboarding"
    - "Les JWT claims (org_id, role) sont présents dans le cookie après étape 2"
  artifacts:
    - path: middleware.ts
      provides: "Guard actif sur toutes les routes"
    - path: src/app/login/page.tsx
      provides: "Page login accessible"
    - path: src/app/onboarding/page.tsx
      provides: "Wizard onboarding complet"
    - path: src/app/dashboard/page.tsx
      provides: "Dashboard stub accessible"
  key_links:
    - from: /login
      to: /auth/callback
      via: "magic link email → click → PKCE exchange"
      pattern: "auth/callback"
    - from: /auth/callback
      to: /onboarding
      via: "is_complete = false → redirect"
      pattern: "is_complete"
    - from: /onboarding step 2
      to: app_metadata.org_id
      via: "saveCabinetStep → updateUserById → refreshSession"
      pattern: "org_id"
---

<objective>
Smoke test manuel complet du flow Phase 2 : magic link → onboarding → dashboard → invitation.

Purpose: Valider que AUTH-01 à AUTH-05 et RBAC-01 à RBAC-03 fonctionnent de bout en bout avant de marquer Phase 2 complète.
Output: Confirmation humaine ou liste d'issues à corriger.
</objective>

<execution_context>
@/Users/zakariabillouche/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zakariabillouche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authentification-rbac/02-VALIDATION.md
@.planning/phases/02-authentification-rbac/02-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lancer le serveur de dev + exécuter les tests DB automatisés</name>
  <files></files>
  <read_first>
    - .planning/phases/02-authentification-rbac/02-VALIDATION.md (checklist complète)
  </read_first>
  <action>
Exécuter les vérifications automatisées avant le smoke test manuel :

```bash
# 1. Vérifier que les tests pgTAP passent
supabase test db

# 2. Vérifier que le build est propre
bun run build

# 3. Lancer le serveur de développement
bun run dev
```

Si une des commandes échoue, corriger les erreurs AVANT de passer au checkpoint humain.

**État attendu avant le checkpoint:**
- `supabase test db` : tous les tests verts (Phase 1 + Phase 2)
- `bun run build` : aucune erreur TypeScript
- Serveur dev actif sur `http://localhost:3000`
  </action>
  <verify>
    <automated>cd /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal && supabase test db 2>&1 | tail -5 && bun run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `supabase test db` sort avec `# Looks like you passed all your tests` ou sans ligne `not ok`
    - `bun run build` sort avec `✓ Compiled` ou sans `Error:`
    - Serveur dev démarre sans erreur console critique
  </acceptance_criteria>
  <done>Tests pgTAP verts, build propre, serveur dev actif. Prêt pour le smoke test humain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Flow complet Phase 2 — magic link, onboarding wizard 4 étapes, middleware, invitation.
  </what-built>
  <how-to-verify>
**Pré-requis :** Serveur dev actif sur `http://localhost:3000`. Variables d'env dans `.env.local` configurées (Supabase URL, anon key, service role key, Resend API key, NEXT_PUBLIC_SITE_URL).

---

**Test 1 — AUTH-03 : Middleware bloque les routes protégées (2 min)**
1. Ouvrir `http://localhost:3000/dashboard` sans être connecté
2. Attendu : redirect automatique vers `http://localhost:3000/login`
3. Ouvrir `http://localhost:3000/onboarding` sans session
4. Attendu : redirect vers `/login`

---

**Test 2 — AUTH-01 : Login magic link (5 min)**
1. Aller sur `http://localhost:3000/login`
2. Vérifier le layout split-screen (formulaire gauche, gradient indigo droit)
3. Saisir une adresse email valide
4. Cliquer "Recevoir mon lien de connexion"
5. Attendu : écran de confirmation in-page avec l'email affiché et "Vérifiez votre boîte mail"
6. Vérifier la réception de l'email magic link
7. Cliquer sur le lien dans l'email
8. Attendu : redirect vers `/onboarding` (profil incomplet)

---

**Test 3 — AUTH-02 + AUTH-05 : Onboarding wizard (10 min)**
1. Être sur `/onboarding` après le magic link
2. Step 1 (Profil) :
   - Remplir Prénom (ex: "Jean"), Nom (ex: "Dupont"), Téléphone (optionnel)
   - Tenter de passer sans Prénom → vérifier message d'erreur en français
   - Cliquer "Continuer" → vérifier transition slide gauche vers Step 2
3. Step 2 (Cabinet) :
   - Remplir "Cabinet Dupont Test"
   - Cliquer "Continuer"
   - Attendu : transition vers Step 3, JWT claims mis à jour
   - Ouvrir DevTools → Application → Cookies → décoder le JWT supabase
   - Vérifier que `app_metadata.org_id` est présent et `app_metadata.role = "expert-comptable"`
4. Step 3 (Pennylane) :
   - Cliquer "Configurer plus tard" (skip)
   - Attendu : transition vers Step 4
5. Step 4 (Sync) :
   - Cliquer "Passer et aller au dashboard"
   - Attendu : redirect vers `/dashboard`

---

**Test 4 — AUTH-03 : Gate profil obligatoire (2 min)**
1. Se déconnecter (supprimer les cookies Supabase depuis DevTools → Application → Cookies)
2. Se reconnecter via magic link avec le même email
3. Attendu : redirect vers `/dashboard` (profil désormais complet, `is_complete = true`)
4. Vérifier que `/dashboard` affiche le prénom/nom et le rôle

---

**Test 5 — RBAC-01 : JWT claims expert-comptable (2 min)**
1. Être connecté en tant qu'expert-comptable sur le dashboard
2. DevTools → Application → Cookies → `sb-xxx-auth-token`
3. Décoder la valeur (base64 → JSON)
4. Vérifier dans `app_metadata` : `org_id` non null, `role: "expert-comptable"`

---

**Test 6 — AUTH-04 : Flow d'invitation (5 min) — optionnel si Resend configuré**
1. Depuis le dashboard, appeler manuellement `POST /api/invite` :
   ```bash
   curl -X POST http://localhost:3000/api/invite \
     -H "Content-Type: application/json" \
     -H "Cookie: [copier les cookies de session depuis DevTools]" \
     -d '{"email": "test-invite@example.com", "role": "collaborateur", "cabinet_ids": []}'
   ```
2. Attendu : `{"success":true,"message":"Invitation envoyée à test-invite@example.com"}`
3. Si Resend configuré : vérifier la réception de l'email d'invitation brandé CabinetPilot

---

**Test 7 — AUTH-01 : Page login erreurs (2 min)**
1. Retourner sur `/login` (déconnecté)
2. Soumettre avec un email invalide (ex: "notanemail")
3. Attendu : "Adresse e-mail invalide" sous le champ, pas de navigation

---

**Tests pgTAP (automatiques, déjà exécutés en Task 1)**
- `supabase test db` — vérifier que tous les tests Phase 1 et Phase 2 sont verts

  </how-to-verify>
  <resume-signal>
Tapez "approved" si tous les tests passent, ou décrivez les issues rencontrées (avec le numéro du test concerné).
  </resume-signal>
</task>

</tasks>

<verification>
Smoke test complet validé par l'utilisateur. `supabase test db` vert. `bun run build` propre.
</verification>

<success_criteria>
- AUTH-01 : Magic link fonctionne, confirmation in-page affichée, email reçu
- AUTH-02 : Wizard 4 étapes complété, transitions animées, progression sauvegardée
- AUTH-03 : Routes protégées sans session → /login. Profil incomplet → /onboarding
- AUTH-04 : POST /api/invite retourne success (email optionnel si Resend non configuré en dev)
- AUTH-05 : /dashboard affiche prénom, nom, rôle de l'utilisateur connecté
- RBAC-01 : JWT claims `role=expert-comptable` + `org_id` dans app_metadata après étape 2
- RBAC-02 : Architecture posée — RLS collaborateur testée Phase 3 (UI non disponible Phase 2)
- RBAC-03 : Architecture posée — sélecteur multi-groupe Phase 3
- `supabase test db` : 100% vert
- `bun run build` : propre
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentification-rbac/02-08-SUMMARY.md`
Update `.planning/STATE.md` : current_phase = 2, status = complete, completed_phases = 2
</output>
