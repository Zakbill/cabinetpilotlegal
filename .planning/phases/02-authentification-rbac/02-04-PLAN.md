---
phase: 02-authentification-rbac
plan: 04
type: execute
wave: 2
depends_on: [02-03]
files_modified:
  - middleware.ts
  - src/app/auth/callback/route.ts
  - src/app/page.tsx
  - src/app/dashboard/page.tsx
autonomous: true
requirements: [AUTH-01, AUTH-03, RBAC-01, RBAC-02, RBAC-03]

must_haves:
  truths:
    - "Une requête sur une route protégée sans session redirige vers `/login`"
    - "Une session avec `profiles.is_complete = false` redirige vers `/onboarding` depuis n'importe quelle route"
    - "La route `/auth/callback` échange le code PKCE et redirige vers `/onboarding` ou `/dashboard` selon `is_complete`"
    - "La page `/dashboard` est un stub accessible uniquement aux profils complets"
  artifacts:
    - path: middleware.ts
      provides: "Auth guard + profile gate pour toutes les routes"
      contains: "updateSession"
    - path: src/app/auth/callback/route.ts
      provides: "Échange PKCE + redirect logic"
      contains: "exchangeCodeForSession"
  key_links:
    - from: middleware.ts
      to: profiles.is_complete
      via: "SELECT is_complete FROM profiles WHERE id = user.id"
      pattern: "is_complete"
    - from: src/app/auth/callback/route.ts
      to: profiles.is_complete
      via: "supabase.from('profiles').select('is_complete').eq('id', user.id)"
      pattern: "is_complete"
---

<objective>
Middleware d'authentification complet + route callback PKCE + stub dashboard. Établit les redirections selon l'état du profil (`is_complete`).

Purpose: Sans ce middleware, les routes protégées sont accessibles sans session. Sans le callback, le magic link ne fonctionne pas.
Output: Guard auth fonctionnel sur toutes les routes + callback PKCE opérationnel.
</objective>

<execution_context>
@/Users/zakariabillouche/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zakariabillouche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authentification-rbac/02-CONTEXT.md
@.planning/phases/02-authentification-rbac/02-RESEARCH.md
@.planning/phases/02-authentification-rbac/02-03-SUMMARY.md

<interfaces>
<!-- updateSession helper — depuis src/lib/supabase/middleware.ts (Plan 03) -->
```typescript
export async function updateSession(request: NextRequest): Promise<{
  supabase: SupabaseClient,
  response: NextResponse,
  user: User | null
}>
```

<!-- createClient server — depuis src/lib/supabase/server.ts (Plan 03) -->
```typescript
export async function createClient(): Promise<SupabaseClient>
```

<!-- Schéma profiles (après migration Plan 01) -->
```sql
profiles: {
  id: uuid,
  organization_id: uuid | null,  -- NULL avant onboarding
  role: 'expert-comptable' | 'collaborateur' | null,
  is_complete: boolean,          -- false par défaut (Plan 01)
  current_onboarding_step: 1|2|3|4, -- 1 par défaut (Plan 01)
  first_name: text | null,
  last_name: text | null
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Middleware auth — session guard + profile gate</name>
  <files>middleware.ts</files>
  <read_first>
    - src/lib/supabase/middleware.ts (updateSession helper — créé Plan 03)
    - .planning/phases/02-authentification-rbac/02-RESEARCH.md §Pattern 2 (middleware exact avec redirects)
    - .planning/phases/02-authentification-rbac/02-CONTEXT.md §Middleware auth (routes publiques)
  </read_first>
  <action>
Créer `middleware.ts` à la RACINE du projet (pas dans `src/`) :

```typescript
import { type NextRequest, NextResponse } from 'next/server'
import { updateSession } from '@/lib/supabase/middleware'

// Routes accessibles sans session active
const PUBLIC_ROUTES = ['/login', '/auth/callback', '/']

export async function middleware(request: NextRequest) {
  const { response, user, supabase } = await updateSession(request)

  const pathname = request.nextUrl.pathname

  // Routes publiques — pas de vérification de session
  if (PUBLIC_ROUTES.some(route => pathname === route || pathname.startsWith(`${route}/`))) {
    // Si déjà connecté avec profil complet → ne pas bloquer sur /login
    if (pathname === '/login' && user) {
      // Vérifier is_complete pour éviter de boucler
      const { data: profile } = await supabase
        .from('profiles')
        .select('is_complete')
        .eq('id', user.id)
        .single()

      if (profile?.is_complete) {
        return NextResponse.redirect(new URL('/dashboard', request.url))
      }
      // is_complete false → laisser sur /login (sera redirigé vers /onboarding après callback)
    }
    return response
  }

  // Route protégée sans session → /login
  if (!user) {
    return NextResponse.redirect(new URL('/login', request.url))
  }

  // Session existante mais profil incomplet → /onboarding
  // Exception : /onboarding lui-même ne doit pas boucler
  if (pathname !== '/onboarding' && !pathname.startsWith('/onboarding')) {
    const { data: profile } = await supabase
      .from('profiles')
      .select('is_complete')
      .eq('id', user.id)
      .single()

    if (!profile?.is_complete) {
      return NextResponse.redirect(new URL('/onboarding', request.url))
    }
  }

  return response
}

export const config = {
  matcher: [
    /*
     * Match toutes les routes SAUF :
     * - _next/static (assets statiques)
     * - _next/image (optimisation images)
     * - favicon.ico
     * - fichiers avec extensions (svg, png, jpg, jpeg, gif, webp)
     */
    '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
  ],
}
```

**Note sur les performances:** La requête `SELECT is_complete FROM profiles` dans le middleware est intentionnelle pour Phase 2. Elle est acceptable car `getUser()` fait déjà un aller-retour Supabase. L'index `idx_profiles_id` (Phase 1) garantit la rapidité de cette requête.
  </action>
  <verify>
    <automated>cd /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal && ls middleware.ts && grep "updateSession" middleware.ts && grep "is_complete" middleware.ts && bun run build 2>&1 | grep -E "error|✓ Compiled" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `middleware.ts` existe à la racine (pas dans `src/`)
    - `middleware.ts` contient `updateSession`
    - `middleware.ts` contient `is_complete`
    - `middleware.ts` contient `PUBLIC_ROUTES` avec `/login`, `/auth/callback`, `/`
    - `middleware.ts` contient `getUser()` (indirectement via updateSession — pas `getSession()`)
    - `middleware.ts` contient `export const config` avec `matcher`
    - `middleware.ts` NE contient PAS `getSession`
    - `bun run build` ne signale pas d'erreur TypeScript sur middleware.ts
  </acceptance_criteria>
  <done>Middleware actif : routes sans session redirigées vers `/login`, profils incomplets redirigés vers `/onboarding`, utilisateurs complets déjà connectés redirigés de `/login` vers `/dashboard`.</done>
</task>

<task type="auto">
  <name>Task 2: Auth callback route + stub dashboard</name>
  <files>
    src/app/auth/callback/route.ts
    src/app/page.tsx
    src/app/dashboard/page.tsx
  </files>
  <read_first>
    - .planning/phases/02-authentification-rbac/02-RESEARCH.md §Pattern 3 (callback PKCE exact)
    - src/lib/supabase/server.ts (createClient server — créé Plan 03)
  </read_first>
  <action>
**Fichier 1 — `src/app/auth/callback/route.ts`:**

```typescript
import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'

export async function GET(request: Request) {
  const { searchParams, origin } = new URL(request.url)
  const code = searchParams.get('code')
  // 'next' param optionnel — utilisé pour deep link après login
  const next = searchParams.get('next') ?? '/dashboard'

  if (code) {
    const supabase = await createClient()
    const { error } = await supabase.auth.exchangeCodeForSession(code)

    if (!error) {
      const { data: { user } } = await supabase.auth.getUser()

      if (user) {
        // Détecter les métadonnées d'invitation dans user_metadata
        const inviteMeta = user.user_metadata as {
          organization_id?: string
          role?: 'expert-comptable' | 'collaborateur'
          cabinet_ids?: string[]
          invited_by?: string
        }

        if (inviteMeta?.organization_id) {
          // Utilisateur invité — pré-assigner org + role + cabinets dans profiles
          await supabase.from('profiles').update({
            organization_id: inviteMeta.organization_id,
            role: inviteMeta.role ?? 'collaborateur',
          }).eq('id', user.id)

          // Pré-assigner les cabinets si fournis
          if (inviteMeta.cabinet_ids && inviteMeta.cabinet_ids.length > 0) {
            const assignments = inviteMeta.cabinet_ids.map(cabinetId => ({
              user_id: user.id,
              cabinet_id: cabinetId,
            }))
            await supabase.from('user_cabinet_assignments').insert(assignments)
          }

          // Les JWT claims (org_id, role) sont mis à jour par le plan 02-07 (Server Action invite)
          // Pour les invités, la mise à jour des claims se fait dans completeOnboarding (plan 02-06)
        }

        // Vérifier is_complete pour la redirection
        const { data: profile } = await supabase
          .from('profiles')
          .select('is_complete')
          .eq('id', user.id)
          .single()

        if (!profile?.is_complete) {
          return NextResponse.redirect(new URL('/onboarding', origin))
        }
      }

      return NextResponse.redirect(new URL(next, origin))
    }
  }

  // Erreur d'échange de code → retour login avec erreur
  return NextResponse.redirect(
    new URL('/login?error=auth_callback_failed', origin)
  )
}
```

**Fichier 2 — `src/app/page.tsx` (root redirect):**

```typescript
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'

export default async function Home() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()

  if (!user) {
    redirect('/login')
  }

  // Le middleware gère déjà le cas is_complete = false → /onboarding
  redirect('/dashboard')
}
```

**Fichier 3 — `src/app/dashboard/page.tsx` (stub Phase 2):**

```typescript
import { createClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'

export default async function DashboardPage() {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()

  if (!user) {
    redirect('/login')
  }

  const { data: profile } = await supabase
    .from('profiles')
    .select('first_name, last_name, role')
    .eq('id', user.id)
    .single()

  return (
    <main className="min-h-screen bg-zinc-50 flex items-center justify-center">
      <div className="text-center">
        <h1 className="font-display text-[32px] font-semibold text-zinc-900">
          CabinetPilot
        </h1>
        <p className="mt-2 text-zinc-600">
          Bienvenue, {profile?.first_name ?? 'utilisateur'} — dashboard Phase 3.
        </p>
        <p className="mt-1 text-sm text-zinc-400">
          Rôle : {profile?.role ?? 'non assigné'}
        </p>
      </div>
    </main>
  )
}
```
  </action>
  <verify>
    <automated>cd /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal && ls src/app/auth/callback/route.ts src/app/dashboard/page.tsx && grep "exchangeCodeForSession" src/app/auth/callback/route.ts && bun run build 2>&1 | grep -E "error TS|✓" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/auth/callback/route.ts` existe et contient `exchangeCodeForSession`
    - `src/app/auth/callback/route.ts` contient `is_complete`
    - `src/app/auth/callback/route.ts` contient `user_metadata` (détection invitations)
    - `src/app/auth/callback/route.ts` contient `/onboarding` comme redirect fallback
    - `src/app/auth/callback/route.ts` contient `auth_callback_failed` comme redirect d'erreur
    - `src/app/dashboard/page.tsx` existe
    - `src/app/page.tsx` contient `redirect('/login')` et `redirect('/dashboard')`
    - `bun run build` sort sans erreur TypeScript sur ces fichiers
  </acceptance_criteria>
  <done>Callback PKCE fonctionnel avec détection invitations + redirects selon `is_complete`. Stub dashboard accessible aux profils complets. Root page redirige selon session.</done>
</task>

</tasks>

<verification>
`bun run build` propre. `middleware.ts` existe à la racine avec `updateSession` + redirects `is_complete`. Callback route contient `exchangeCodeForSession` + détection `user_metadata` d'invitation.
</verification>

<success_criteria>
- Route protégée sans session → redirect `/login`
- Session avec `is_complete = false` → redirect `/onboarding`
- `/auth/callback` échange code PKCE, détecte invitations, redirige selon `is_complete`
- `/dashboard` stub accessible aux profils complets
- `bun run build` propre
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentification-rbac/02-04-SUMMARY.md`
</output>
