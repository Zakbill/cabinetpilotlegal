---
phase: 02-authentification-rbac
plan: 07
type: execute
wave: 4
depends_on: [02-06]
files_modified:
  - src/app/api/invite/route.ts
  - src/components/emails/InvitationEmail.tsx
autonomous: true
requirements: [AUTH-04, RBAC-01, RBAC-02]

must_haves:
  truths:
    - "Un expert-comptable peut inviter un collaborateur ou un autre expert-comptable par e-mail"
    - "L'invité reçoit un e-mail branded CabinetPilot en français avec un magic link"
    - "Le magic link de l'invité l'amène dans le bon groupe avec le bon rôle après callback"
    - "Les cabinets assignés sont pré-configurés dans `user_cabinet_assignments` lors du callback"
  artifacts:
    - path: src/app/api/invite/route.ts
      provides: "POST /api/invite — invitation via Supabase admin + Resend"
      contains: "inviteUserByEmail"
    - path: src/components/emails/InvitationEmail.tsx
      provides: "Template e-mail d'invitation React Email"
  key_links:
    - from: src/app/api/invite/route.ts
      to: supabase.auth.admin.inviteUserByEmail
      via: "data: { organization_id, role, cabinet_ids, invited_by }"
      pattern: "inviteUserByEmail"
    - from: src/app/auth/callback/route.ts
      to: user_cabinet_assignments
      via: "INSERT into user_cabinet_assignments (user_id, cabinet_id) — Plan 04"
      pattern: "user_cabinet_assignments"
---

<objective>
Route Handler d'invitation + template email React Email. Permet à un expert-comptable d'inviter des membres via POST /api/invite.

Purpose: AUTH-04 complet. Le flow d'invitation utilise `auth.admin.inviteUserByEmail` avec `user_metadata` pour passer les métadonnées — le callback (Plan 04) les détecte et pré-configure le profil invité.
Output: POST /api/invite fonctionnel + email branded envoyé via Resend.
</objective>

<execution_context>
@/Users/zakariabillouche/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zakariabillouche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authentification-rbac/02-RESEARCH.md
@.planning/phases/02-authentification-rbac/02-UI-SPEC.md
@.planning/phases/02-authentification-rbac/02-06-SUMMARY.md

<interfaces>
<!-- inviteUserByEmail pattern (RESEARCH.md Pattern 6) -->
```typescript
await supabaseAdmin.auth.admin.inviteUserByEmail(inviteeEmail, {
  redirectTo: `${SITE_URL}/auth/callback`,
  data: {  // -> user_metadata
    invited_by: inviterId,
    organization_id: orgId,
    role: 'collaborateur' | 'expert-comptable',
    cabinet_ids: string[],
  }
})
```

<!-- Resend + React Email pattern (RESEARCH.md Pattern 8) -->
```typescript
import { Resend } from 'resend'
await resend.emails.send({
  from: 'CabinetPilot <noreply@cabinetpilot.io>',
  to: [inviteeEmail],
  subject: `Vous êtes invité à rejoindre ${orgName} sur CabinetPilot`,
  react: InvitationEmail({ orgName, inviterName }),
})
```

<!-- Copywriting UI-SPEC -->
<!-- Subject: "Vous êtes invité à rejoindre {org_name} sur CabinetPilot" -->
<!-- CTA email: "Rejoindre {org_name}" -->
<!-- Success toast: "Invitation envoyée à {email}" -->
<!-- Error: "Impossible d'envoyer l'invitation. Vérifiez l'adresse e-mail et réessayez." -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route Handler POST /api/invite + template email React Email</name>
  <files>
    src/app/api/invite/route.ts
    src/components/emails/InvitationEmail.tsx
  </files>
  <read_first>
    - .planning/phases/02-authentification-rbac/02-RESEARCH.md §Pattern 6 (inviteUserByEmail exact)
    - .planning/phases/02-authentification-rbac/02-RESEARCH.md §Pattern 8 (Resend + React Email)
    - .planning/phases/02-authentification-rbac/02-UI-SPEC.md §Copywriting Contract (copy invitation FR)
    - src/app/auth/callback/route.ts (détecte user_metadata — doit être cohérent avec data passé ici)
  </read_first>
  <action>
**Fichier 1 — `src/app/api/invite/route.ts`:**

```typescript
import { NextResponse } from 'next/server'
import { createClient as createAdminClient } from '@supabase/supabase-js'
import { createClient } from '@/lib/supabase/server'
import { Resend } from 'resend'
import { InvitationEmail } from '@/components/emails/InvitationEmail'

const resend = new Resend(process.env.RESEND_API_KEY)

function getAdminClient() {
  return createAdminClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { auth: { autoRefreshToken: false, persistSession: false } }
  )
}

interface InvitePayload {
  email: string
  role: 'expert-comptable' | 'collaborateur'
  cabinet_ids?: string[]
}

export async function POST(request: Request) {
  // Vérifier que l'appelant est authentifié et est expert-comptable
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()

  if (!user) {
    return NextResponse.json({ error: 'Non authentifié' }, { status: 401 })
  }

  // Vérifier le rôle de l'invitant
  const { data: inviterProfile } = await supabase
    .from('profiles')
    .select('role, organization_id, first_name, last_name')
    .eq('id', user.id)
    .single()

  if (inviterProfile?.role !== 'expert-comptable') {
    return NextResponse.json({ error: 'Seul un expert-comptable peut inviter des membres' }, { status: 403 })
  }

  if (!inviterProfile?.organization_id) {
    return NextResponse.json({ error: 'Organisation non configurée' }, { status: 400 })
  }

  // Valider le payload
  let payload: InvitePayload
  try {
    payload = await request.json()
  } catch {
    return NextResponse.json({ error: 'Corps de la requête invalide' }, { status: 400 })
  }

  const { email, role, cabinet_ids = [] } = payload

  if (!email || !email.includes('@')) {
    return NextResponse.json({ error: 'Adresse e-mail invalide' }, { status: 400 })
  }

  if (!['expert-comptable', 'collaborateur'].includes(role)) {
    return NextResponse.json({ error: 'Rôle invalide' }, { status: 400 })
  }

  // Récupérer le nom de l'organisation pour l'email
  const { data: org } = await supabase
    .from('organizations')
    .select('name')
    .eq('id', inviterProfile.organization_id)
    .single()

  const orgName = org?.name ?? 'votre organisation'
  const inviterName = [inviterProfile.first_name, inviterProfile.last_name].filter(Boolean).join(' ') || 'Un expert-comptable'

  // Envoyer l'invitation via Supabase Admin
  const supabaseAdmin = getAdminClient()
  const { data: inviteData, error: inviteError } = await supabaseAdmin.auth.admin.inviteUserByEmail(
    email,
    {
      redirectTo: `${process.env.NEXT_PUBLIC_SITE_URL}/auth/callback`,
      data: {
        // Ces données sont lues par /auth/callback pour pré-configurer le profil invité
        invited_by: user.id,
        organization_id: inviterProfile.organization_id,
        role,
        cabinet_ids,
      },
    }
  )

  if (inviteError) {
    // Cas spécial : utilisateur déjà inscrit
    if (inviteError.message.includes('already been invited') || inviteError.message.includes('already registered')) {
      return NextResponse.json({
        error: 'Impossible d\'envoyer l\'invitation. Vérifiez l\'adresse e-mail et réessayez.',
      }, { status: 400 })
    }
    return NextResponse.json({ error: 'Impossible d\'envoyer l\'invitation. Vérifiez l\'adresse e-mail et réessayez.' }, { status: 500 })
  }

  // Générer le magic link pour l'email brandé
  // Note: inviteUserByEmail envoie l'email par défaut via Supabase.
  // On envoie AUSSI un email brandé via Resend pour le design.
  // Pour éviter un double email, configurer Supabase pour désactiver l'email natif d'invitation
  // (Supabase Dashboard -> Auth -> Email Templates -> Invite -> vider le template)
  try {
    await resend.emails.send({
      from: 'CabinetPilot <noreply@cabinetpilot.io>',
      to: [email],
      subject: `Vous êtes invité à rejoindre ${orgName} sur CabinetPilot`,
      react: InvitationEmail({
        orgName,
        inviterName,
        // Le magic link est dans inviteData mais non exposé directement par inviteUserByEmail
        // L'email Supabase natif contient le lien — cet email est un complément de notification
      }) as React.ReactElement,
    })
  } catch (resendError) {
    // Email Resend optionnel — ne pas bloquer si Resend échoue
    console.error('Resend error (non-blocking):', resendError)
  }

  return NextResponse.json({
    success: true,
    message: `Invitation envoyée à ${email}`,
  })
}
```

**Fichier 2 — `src/components/emails/InvitationEmail.tsx`:**

```typescript
import {
  Html,
  Head,
  Body,
  Container,
  Section,
  Text,
  Button,
  Hr,
  Preview,
} from '@react-email/components'

interface InvitationEmailProps {
  orgName: string
  inviterName: string
}

export function InvitationEmail({ orgName, inviterName }: InvitationEmailProps) {
  return (
    <Html lang="fr">
      <Head />
      <Preview>Vous êtes invité à rejoindre {orgName} sur CabinetPilot</Preview>
      <Body style={{ backgroundColor: '#fafafa', fontFamily: 'DM Sans, system-ui, sans-serif', margin: 0 }}>
        <Container style={{ maxWidth: '520px', margin: '40px auto', backgroundColor: '#ffffff', borderRadius: '8px', border: '1px solid #e4e4e7', padding: '40px' }}>
          {/* Logo */}
          <Text style={{ fontSize: '18px', fontWeight: 600, color: '#18181b', margin: '0 0 32px' }}>
            CabinetPilot
          </Text>

          {/* Heading */}
          <Text style={{ fontSize: '22px', fontWeight: 600, color: '#18181b', margin: '0 0 16px' }}>
            Vous avez été invité à rejoindre {orgName}
          </Text>

          <Text style={{ fontSize: '16px', color: '#52525b', lineHeight: 1.5, margin: '0 0 24px' }}>
            {inviterName} vous invite à rejoindre {orgName} sur CabinetPilot — la plateforme de suivi des missions juridiques pour les cabinets comptables.
          </Text>

          {/* CTA Button */}
          <Section style={{ textAlign: 'center', margin: '32px 0' }}>
            <Button
              style={{
                backgroundColor: '#4f46e5',
                color: '#ffffff',
                borderRadius: '8px',
                padding: '14px 28px',
                fontSize: '16px',
                fontWeight: 600,
                textDecoration: 'none',
                display: 'inline-block',
              }}
              href="#"
            >
              Rejoindre {orgName}
            </Button>
          </Section>

          <Text style={{ fontSize: '14px', color: '#71717a', margin: '0 0 8px' }}>
            Ce lien expirera dans 24 heures. Si vous n&apos;avez pas demandé cette invitation, vous pouvez ignorer cet e-mail.
          </Text>

          <Hr style={{ borderColor: '#e4e4e7', margin: '24px 0' }} />

          <Text style={{ fontSize: '12px', color: '#a1a1aa', margin: 0 }}>
            CabinetPilot · Pilotez vos missions juridiques en un coup d&apos;œil
          </Text>
        </Container>
      </Body>
    </Html>
  )
}
```
  </action>
  <verify>
    <automated>cd /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal && ls src/app/api/invite/route.ts src/components/emails/InvitationEmail.tsx && grep "inviteUserByEmail" src/app/api/invite/route.ts && grep "SUPABASE_SERVICE_ROLE_KEY" src/app/api/invite/route.ts && bun run build 2>&1 | grep -E "error TS|✓ Compiled" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/invite/route.ts` contient `inviteUserByEmail`
    - `src/app/api/invite/route.ts` contient `SUPABASE_SERVICE_ROLE_KEY` (sans NEXT_PUBLIC_)
    - `src/app/api/invite/route.ts` contient `organization_id` dans `data` (user_metadata payload)
    - `src/app/api/invite/route.ts` contient `cabinet_ids` dans `data` (user_metadata payload)
    - `src/app/api/invite/route.ts` contient `role: 'expert-comptable'` guard (403 si non expert-comptable)
    - `src/app/api/invite/route.ts` contient `Impossible d'envoyer l'invitation` (message erreur FR)
    - `src/app/api/invite/route.ts` contient `Invitation envoyée à` (message success FR)
    - `src/components/emails/InvitationEmail.tsx` contient `@react-email/components`
    - `src/components/emails/InvitationEmail.tsx` contient `Rejoindre {orgName}` (copie UI-SPEC)
    - `src/components/emails/InvitationEmail.tsx` contient `lang="fr"`
    - `bun run build` sans erreur TypeScript critique
  </acceptance_criteria>
  <done>POST /api/invite opérationnel — vérifie le rôle de l'invitant, appelle `inviteUserByEmail` avec métadonnées (org, role, cabinets), envoie email brandé via Resend. Messages d'erreur/succès en français conformes à UI-SPEC.</done>
</task>

</tasks>

<verification>
`bun run build` propre. `src/app/api/invite/route.ts` contient `inviteUserByEmail` + `SUPABASE_SERVICE_ROLE_KEY`. Le callback `/auth/callback` (Plan 04) est cohérent avec les champs `data` passés ici (`organization_id`, `role`, `cabinet_ids`).
</verification>

<success_criteria>
- POST /api/invite accepte `{ email, role, cabinet_ids }` et renvoie `{ success: true, message }` ou `{ error }`
- Seuls les experts-comptables peuvent invoquer cette route (403 sinon)
- `inviteUserByEmail` passe `organization_id`, `role`, `cabinet_ids` en `user_metadata`
- Email brandé React Email envoyé via Resend en français
- `bun run build` propre
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentification-rbac/02-07-SUMMARY.md`
</output>
