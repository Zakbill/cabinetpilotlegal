---
phase: 02-authentification-rbac
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20260316000008_add_onboarding_columns.sql
  - supabase/migrations/20260316000009_add_profiles_trigger.sql
  - supabase/tests/02_onboarding_columns.test.sql
  - supabase/tests/02_profiles_trigger.test.sql
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-04, AUTH-05]

must_haves:
  truths:
    - "La table `profiles` contient les colonnes `is_complete` (boolean not null default false) et `current_onboarding_step` (integer not null default 1)"
    - "Un INSERT dans `auth.users` crée automatiquement une ligne dans `profiles` via trigger"
    - "Les tests pgTAP passent en vert sur `supabase test db`"
  artifacts:
    - path: supabase/migrations/20260316000008_add_onboarding_columns.sql
      provides: "Colonnes onboarding dans profiles"
      contains: "is_complete"
    - path: supabase/migrations/20260316000009_add_profiles_trigger.sql
      provides: "Trigger handle_new_user sur auth.users"
      contains: "on_auth_user_created"
    - path: supabase/tests/02_onboarding_columns.test.sql
      provides: "pgTAP validation colonnes onboarding"
    - path: supabase/tests/02_profiles_trigger.test.sql
      provides: "pgTAP validation trigger profil"
  key_links:
    - from: auth.users
      to: public.profiles
      via: "AFTER INSERT trigger on_auth_user_created"
      pattern: "on_auth_user_created"
    - from: middleware.ts
      to: profiles.is_complete
      via: "SELECT is_complete FROM profiles WHERE id = auth.uid()"
      pattern: "is_complete"
---

<objective>
Pose la fondation DB de Phase 2 : migration des colonnes onboarding dans `profiles` + trigger auto-création profil + stubs pgTAP.

Purpose: Le middleware auth et l'onboarding wizard dépendent de `profiles.is_complete` et `profiles.current_onboarding_step`. Le trigger garantit qu'un profil existe avant que le callback ne le lise.
Output: 2 migrations SQL + 2 fichiers de tests pgTAP passant en vert.
</objective>

<execution_context>
@/Users/zakariabillouche/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zakariabillouche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-authentification-rbac/02-CONTEXT.md
@.planning/phases/02-authentification-rbac/02-RESEARCH.md
@.planning/phases/02-authentification-rbac/02-VALIDATION.md

<interfaces>
<!-- Schéma actuel de profiles (Phase 1) — à compléter avec les colonnes Phase 2 -->
<!-- Source: supabase/migrations/20260316000002_create_profiles_cabinets.sql -->
```sql
create table public.profiles (
  id              uuid primary key references auth.users(id) on delete cascade,
  organization_id uuid references public.organizations(id) on delete cascade,
  role            public.user_role,  -- NULL avant onboarding
  first_name      text,
  last_name       text,
  phone           text,
  avatar_url      text,
  created_at      timestamptz not null default now()
);
```

<!-- Pattern pgTAP Phase 1 (à reproduire) -->
<!-- Source: supabase/tests/01_schema.test.sql pattern établi -->
```sql
begin;
select plan(N);
-- tests ici
select * from finish();
rollback;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — colonnes onboarding + trigger handle_new_user</name>
  <files>
    supabase/migrations/20260316000008_add_onboarding_columns.sql
    supabase/migrations/20260316000009_add_profiles_trigger.sql
  </files>
  <read_first>
    - supabase/migrations/20260316000002_create_profiles_cabinets.sql (schéma actuel profiles)
    - supabase/migrations/20260316000007_enable_rls_policies.sql (dernier fichier de migration — vérifier que le timestamp 000008 est bien le prochain)
    - supabase/tests/01_schema.test.sql (pattern pgTAP à reproduire)
  </read_first>
  <action>
**Fichier 1 — `supabase/migrations/20260316000008_add_onboarding_columns.sql`:**

```sql
-- Migration 008: Add onboarding columns to profiles
-- Phase 2 — Authentification & RBAC

alter table public.profiles
  add column is_complete             boolean not null default false,
  add column current_onboarding_step integer not null default 1
    check (current_onboarding_step between 1 and 4);

comment on column public.profiles.is_complete is 'true = profil complété, accès dashboard autorisé. false = redirigé vers /onboarding par le middleware.';
comment on column public.profiles.current_onboarding_step is 'Étape courante du wizard onboarding (1=Profil, 2=Cabinet, 3=Pennylane, 4=Sync). Sauvegardé à chaque étape pour reprise.';
```

**Fichier 2 — `supabase/migrations/20260316000009_add_profiles_trigger.sql`:**

```sql
-- Migration 009: Auto-create profile on auth.users INSERT
-- Phase 2 — Authentification & RBAC

-- Fonction trigger : crée un profil minimal (id seulement) à chaque nouvel utilisateur auth
create or replace function public.handle_new_user()
returns trigger
language plpgsql
security definer
set search_path = public
as $$
begin
  insert into public.profiles (id)
  values (new.id)
  on conflict (id) do nothing;  -- idempotent : si profil déjà présent, ne pas crasher
  return new;
end;
$$;

-- Trigger : après chaque INSERT dans auth.users
create trigger on_auth_user_created
  after insert on auth.users
  for each row execute procedure public.handle_new_user();

comment on function public.handle_new_user() is 'Crée automatiquement un profil minimal dans public.profiles lors de l''inscription ou invitation d''un utilisateur.';
```

**Important:** `security definer` permet au trigger d'insérer dans `profiles` même si la RLS bloque les inserts anonymes. Le `on conflict do nothing` rend le trigger idempotent.
  </action>
  <verify>
    <automated>cd /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal && supabase db reset --local 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260316000008_add_onboarding_columns.sql` contient `add column is_complete boolean not null default false`
    - `supabase/migrations/20260316000008_add_onboarding_columns.sql` contient `add column current_onboarding_step integer not null default 1`
    - `supabase/migrations/20260316000008_add_onboarding_columns.sql` contient `check (current_onboarding_step between 1 and 4)`
    - `supabase/migrations/20260316000009_add_profiles_trigger.sql` contient `create trigger on_auth_user_created`
    - `supabase/migrations/20260316000009_add_profiles_trigger.sql` contient `security definer`
    - `supabase/migrations/20260316000009_add_profiles_trigger.sql` contient `on conflict (id) do nothing`
    - `supabase db reset --local` sort sans erreur (exit code 0)
  </acceptance_criteria>
  <done>Les deux migrations s'appliquent proprement sur un `db reset`. La colonne `is_complete` et le trigger `on_auth_user_created` existent dans le schéma local.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pgTAP stubs — colonnes onboarding + trigger profil</name>
  <files>
    supabase/tests/02_onboarding_columns.test.sql
    supabase/tests/02_profiles_trigger.test.sql
  </files>
  <read_first>
    - supabase/tests/01_schema.test.sql (pattern pgTAP exact à reproduire — structure begin/plan/finish/rollback)
    - supabase/migrations/20260316000008_add_onboarding_columns.sql (ce qu'on veut tester)
    - supabase/migrations/20260316000009_add_profiles_trigger.sql (trigger à tester)
  </read_first>
  <behavior>
    - Test 1: `profiles` has column `is_complete` of type `boolean`
    - Test 2: `profiles.is_complete` has default `false`
    - Test 3: `profiles` has column `current_onboarding_step` of type `integer`
    - Test 4: `current_onboarding_step` check constraint rejects values outside 1-4
    - Test 5: trigger `on_auth_user_created` exists on `auth.users`
    - Test 6: inserting a row into `auth.users` creates a corresponding row in `profiles`
  </behavior>
  <action>
**Fichier 1 — `supabase/tests/02_onboarding_columns.test.sql`:**

```sql
-- pgTAP tests: onboarding columns in profiles
-- Phase 2 — AUTH-01, AUTH-02

begin;
select plan(4);

-- Test 1: is_complete column exists with correct type
select col_type_is(
  'public', 'profiles', 'is_complete',
  'boolean',
  'profiles.is_complete est de type boolean'
);

-- Test 2: is_complete default is false
select col_default_is(
  'public', 'profiles', 'is_complete',
  'false',
  'profiles.is_complete a pour défaut false'
);

-- Test 3: current_onboarding_step column exists with correct type
select col_type_is(
  'public', 'profiles', 'current_onboarding_step',
  'integer',
  'profiles.current_onboarding_step est de type integer'
);

-- Test 4: current_onboarding_step default is 1
select col_default_is(
  'public', 'profiles', 'current_onboarding_step',
  '1',
  'profiles.current_onboarding_step a pour défaut 1'
);

select * from finish();
rollback;
```

**Fichier 2 — `supabase/tests/02_profiles_trigger.test.sql`:**

```sql
-- pgTAP tests: profiles trigger on auth.users insert
-- Phase 2 — AUTH-01

begin;
select plan(2);

-- Test 1: trigger exists
select trigger_is(
  'auth', 'users', 'on_auth_user_created',
  'public', 'handle_new_user',
  'trigger on_auth_user_created existe sur auth.users'
);

-- Test 2: inserting into auth.users creates a profile
do $$
declare
  v_user_id uuid := gen_random_uuid();
begin
  -- Insérer un utilisateur minimal dans auth.users
  insert into auth.users (
    id, email, encrypted_password, email_confirmed_at,
    raw_app_meta_data, raw_user_meta_data,
    created_at, updated_at, aud, role
  ) values (
    v_user_id, 'test-trigger@cabinetpilot.test', '', now(),
    '{}'::jsonb, '{}'::jsonb,
    now(), now(), 'authenticated', 'authenticated'
  );
end;
$$;

select results_eq(
  'select count(*)::int from public.profiles where id = (select id from auth.users where email = ''test-trigger@cabinetpilot.test'')',
  array[1],
  'le trigger crée automatiquement un profil dans public.profiles'
);

select * from finish();
rollback;
```
  </action>
  <verify>
    <automated>cd /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal && supabase test db 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/tests/02_onboarding_columns.test.sql` contient `select plan(4)`
    - `supabase/tests/02_onboarding_columns.test.sql` contient `col_type_is` pour `is_complete` et `current_onboarding_step`
    - `supabase/tests/02_profiles_trigger.test.sql` contient `trigger_is` pour `on_auth_user_created`
    - `supabase test db` sort avec `# Looks like you passed all your tests` ou `ok` pour les 6 tests
    - `supabase test db` exit code 0
  </acceptance_criteria>
  <done>6 tests pgTAP passent en vert. Les colonnes onboarding et le trigger sont validés automatiquement.</done>
</task>

</tasks>

<verification>
`supabase test db` passe avec 6 tests verts. Les deux migrations s'appliquent sans erreur sur `supabase db reset --local`.
</verification>

<success_criteria>
- `profiles` table contient `is_complete boolean not null default false`
- `profiles` table contient `current_onboarding_step integer not null default 1 check (between 1 and 4)`
- Trigger `on_auth_user_created` crée automatiquement un profil à l'inscription
- `supabase test db` : 6/6 tests verts
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentification-rbac/02-01-SUMMARY.md`
</output>
