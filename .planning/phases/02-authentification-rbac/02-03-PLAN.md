---
phase: 02-authentification-rbac
plan: 03
type: execute
wave: 1
depends_on: [02-02]
files_modified:
  - package.json
  - bun.lockb
  - next.config.ts
  - tailwind.config.ts
  - src/app/layout.tsx
  - src/styles/globals.css
  - src/lib/design-tokens.ts
  - src/lib/supabase/client.ts
  - src/lib/supabase/server.ts
  - src/lib/supabase/middleware.ts
  - .env.local.example
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, RBAC-01, RBAC-02, RBAC-03]

must_haves:
  truths:
    - "Le projet Next.js démarre sans erreur (`bun run dev` sur localhost:3000)"
    - "Les design tokens sont dans `src/lib/design-tokens.ts` — copie exacte du draft"
    - "Les deux clients Supabase sont créés (browser + server) via `@supabase/ssr`"
    - "Cal Sans et DM Sans sont chargées via next/font et appliquées dans globals.css"
    - "Les variables CSS des tokens sont déclarées dans `:root` de globals.css"
  artifacts:
    - path: src/lib/design-tokens.ts
      provides: "Source unique de vérité des tokens"
      exports: [colors, fonts, spacing, sizes, radius, shadows, breakpoints, gradients, states, tokens]
    - path: src/lib/supabase/client.ts
      provides: "Supabase browser client"
      exports: [createClient]
    - path: src/lib/supabase/server.ts
      provides: "Supabase server client"
      exports: [createClient]
    - path: src/lib/supabase/middleware.ts
      provides: "updateSession helper"
      exports: [updateSession]
  key_links:
    - from: src/app/layout.tsx
      to: src/styles/globals.css
      via: "import '@/styles/globals.css'"
      pattern: "globals.css"
    - from: src/styles/globals.css
      to: src/lib/design-tokens.ts
      via: "CSS variables --color-accent: #4f46e5 etc."
      pattern: "--color-"
---

<objective>
Bootstrap complet du projet Next.js : installation des dépendances, structure des dossiers, design tokens, clients Supabase, polices, variables CSS.

Purpose: Fournit la fondation sur laquelle tous les composants UI et logiques auth s'appuient. Doit être complété avant tout plan Wave 2+.
Output: Projet Next.js fonctionnel avec design system et clients Supabase prêts.
</objective>

<execution_context>
@/Users/zakariabillouche/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zakariabillouche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authentification-rbac/02-CONTEXT.md
@.planning/phases/02-authentification-rbac/02-RESEARCH.md
@.planning/phases/02-authentification-rbac/02-UI-SPEC.md
@.planning/phases/02-authentification-rbac/02-02-SUMMARY.md
@.planning/artifacts/design-tokens.ts

<interfaces>
<!-- Tokens à copier verbatim depuis .planning/artifacts/design-tokens.ts -->
<!-- Exports attendus: colors, fonts, spacing, sizes, radius, shadows, breakpoints, gradients, states, tokens -->

<!-- Client browser pattern (RESEARCH.md Pattern 1) -->
```typescript
// src/lib/supabase/client.ts
import { createBrowserClient } from '@supabase/ssr'
export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  )
}
```

<!-- Client server pattern (RESEARCH.md Pattern 1) -->
```typescript
// src/lib/supabase/server.ts
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'
export async function createClient() {
  const cookieStore = await cookies()
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    { cookies: { getAll() { return cookieStore.getAll() }, setAll(cookiesToSet) { try { cookiesToSet.forEach(({ name, value, options }) => cookieStore.set(name, value, options)) } catch {} } } }
  )
}
```

<!-- updateSession pattern (RESEARCH.md Pattern 1) -->
```typescript
// src/lib/supabase/middleware.ts
import { createServerClient } from '@supabase/ssr'
import { NextResponse, type NextRequest } from 'next/server'
export async function updateSession(request: NextRequest) {
  let response = NextResponse.next({ request })
  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    { cookies: { getAll() { return request.cookies.getAll() }, setAll(cookiesToSet) { cookiesToSet.forEach(({ name, value, options }) => { response.cookies.set(name, value, options) }) } } }
  )
  const { data: { user } } = await supabase.auth.getUser()
  return { supabase, response, user }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Next.js + installation dépendances + shadcn init</name>
  <files>
    package.json
    bun.lockb
    next.config.ts
    tailwind.config.ts
    components.json
  </files>
  <read_first>
    - .planning/phases/02-authentification-rbac/02-02-SUMMARY.md (liste approuvée des composants shadcn)
    - .planning/phases/02-authentification-rbac/02-RESEARCH.md §Standard Stack (versions et commandes)
  </read_first>
  <action>
Si le projet Next.js n'existe pas encore (pas de `package.json` à la racine), l'initialiser:

```bash
# Depuis /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal
bun create next-app . --ts --tailwind --src-dir --app --use-bun --yes
```

Si le projet existe déjà (déjà un `package.json`), passer directement à l'installation des packages.

**Installer toutes les dépendances:**
```bash
bun add @supabase/ssr @supabase/supabase-js framer-motion resend @react-email/components
bun add react-hook-form @hookform/resolvers zod next-themes
```

**Initialiser shadcn (New York style):**
```bash
bunx shadcn@latest init
```
Répondre aux questions:
- Style: New York
- Base color: Zinc
- CSS variables: yes

**Installer les composants approuvés (liste depuis 02-02-SUMMARY.md):**
```bash
bunx shadcn@latest add button input label form avatar separator toast
```

**Vérifier `next.config.ts`:** S'assurer que la config est compatible App Router. Aucune modification spécifique requise — garder la config générée par défaut.

**Vérifier `tailwind.config.ts`:** Le fichier généré par shadcn doit déjà inclure les paths `./src/**/*.{ts,tsx}`. Si absent, ajouter:
```typescript
content: [
  './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
  './src/components/**/*.{js,ts,jsx,tsx,mdx}',
  './src/app/**/*.{js,ts,jsx,tsx,mdx}',
],
```
  </action>
  <verify>
    <automated>cd /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal && ls package.json components.json src/ && bun run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contient `"@supabase/ssr"` dans dependencies
    - `package.json` contient `"framer-motion"` dans dependencies
    - `package.json` contient `"resend"` dans dependencies
    - `package.json` contient `"react-hook-form"` dans dependencies
    - `package.json` contient `"zod"` dans dependencies
    - `components.json` existe (créé par `bunx shadcn@latest init`)
    - `components.json` contient `"style": "new-york"`
    - `src/components/ui/button.tsx` existe (créé par shadcn add)
    - `src/components/ui/input.tsx` existe
    - `src/components/ui/form.tsx` existe
    - `bun run build` sort sans erreur TypeScript critique
  </acceptance_criteria>
  <done>Next.js bootstrappé, dépendances installées, shadcn initialisé en style New York, 7 composants shadcn disponibles dans `src/components/ui/`.</done>
</task>

<task type="auto">
  <name>Task 2: Design tokens + polices + clients Supabase + variables CSS</name>
  <files>
    src/lib/design-tokens.ts
    src/lib/supabase/client.ts
    src/lib/supabase/server.ts
    src/lib/supabase/middleware.ts
    src/app/layout.tsx
    src/styles/globals.css
    .env.local.example
  </files>
  <read_first>
    - .planning/artifacts/design-tokens.ts (copier VERBATIM vers src/lib/design-tokens.ts)
    - .planning/phases/02-authentification-rbac/02-UI-SPEC.md §Design System (fonts, colors)
    - .planning/phases/02-authentification-rbac/02-RESEARCH.md §Pattern 1 (clients Supabase exact)
    - src/app/layout.tsx (état actuel généré par Next.js — à modifier)
    - src/styles/globals.css (état actuel généré — à compléter avec CSS variables)
  </read_first>
  <action>
**1. Copier design-tokens.ts verbatim:**
Copier le contenu exact de `.planning/artifacts/design-tokens.ts` vers `src/lib/design-tokens.ts`. Aucune modification.

**2. Créer `src/lib/supabase/client.ts`:**
```typescript
import { createBrowserClient } from '@supabase/ssr'

export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  )
}
```

**3. Créer `src/lib/supabase/server.ts`:**
```typescript
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export async function createClient() {
  const cookieStore = await cookies()

  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return cookieStore.getAll()
        },
        setAll(cookiesToSet) {
          try {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            )
          } catch {
            // Server Component — middleware persiste les cookies
          }
        },
      },
    }
  )
}
```

**4. Créer `src/lib/supabase/middleware.ts`:**
```typescript
import { createServerClient } from '@supabase/ssr'
import { NextResponse, type NextRequest } from 'next/server'

export async function updateSession(request: NextRequest) {
  let response = NextResponse.next({ request })

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return request.cookies.getAll()
        },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value, options }) => {
            response.cookies.set(name, value, options)
          })
        },
      },
    }
  )

  // CRITIQUE: toujours getUser() dans le middleware, jamais getSession()
  const { data: { user } } = await supabase.auth.getUser()

  return { supabase, response, user }
}
```

**5. Modifier `src/app/layout.tsx`:**
Charger Cal Sans depuis CDN et DM Sans depuis next/font. Cal Sans n'est pas sur Google Fonts — charger via `<link>` dans le `<head>`.

```typescript
import type { Metadata } from 'next'
import { DM_Sans } from 'next/font/google'
import '@/styles/globals.css'

const dmSans = DM_Sans({
  subsets: ['latin'],
  weight: ['400', '600'],
  variable: '--font-dm-sans',
  display: 'swap',
})

export const metadata: Metadata = {
  title: 'CabinetPilot',
  description: 'Pilotez vos missions juridiques en un coup d\'œil',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="fr" suppressHydrationWarning>
      <head>
        {/* Cal Sans — non disponible sur Google Fonts, chargé depuis CDN */}
        <link
          rel="stylesheet"
          href="https://fonts.cdnfonts.com/css/cal-sans"
        />
      </head>
      <body className={`${dmSans.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  )
}
```

**6. Compléter `src/styles/globals.css`:**
Garder les directives Tailwind existantes et ajouter les variables CSS des tokens dans `:root`. Ces variables permettent d'utiliser `var(--color-accent)` dans les composants.

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  /* Fonts */
  --font-cal-sans: 'Cal Sans', system-ui, sans-serif;

  /* Colors — design tokens */
  --color-white:    #ffffff;
  --color-zinc-50:  #fafafa;
  --color-zinc-100: #f4f4f5;
  --color-zinc-200: #e4e4e7;
  --color-zinc-300: #d4d4d8;
  --color-zinc-400: #a1a1aa;
  --color-zinc-500: #71717a;
  --color-zinc-600: #52525b;
  --color-zinc-700: #3f3f46;
  --color-zinc-900: #18181b;
  --color-accent:        #4f46e5;  /* indigo-600 */
  --color-accent-hover:  #4338ca;  /* indigo-700 */
  --color-success:       #16a34a;  /* green-600 */
  --color-error:         #ef4444;  /* red-500 */
  --color-destructive:   #dc2626;  /* red-600 */

  /* Gradients */
  --gradient-login-panel: linear-gradient(135deg, #4338ca 0%, #6d28d9 100%);

  /* Sizes */
  --size-button-height:  48px;
  --size-step-circle:    32px;
  --size-avatar-preview: 64px;
  --size-login-max:      360px;
  --size-wizard-max:     520px;
}

/* Heading font utility class */
.font-display {
  font-family: var(--font-cal-sans);
}

/* Ensure DM Sans is applied globally */
body {
  font-family: var(--font-dm-sans), system-ui, sans-serif;
  background-color: var(--color-zinc-50);
  color: var(--color-zinc-900);
}
```

**7. Créer `.env.local.example`:**
```bash
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJxxx
SUPABASE_SERVICE_ROLE_KEY=eyJxxx

# Site URL
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Resend
RESEND_API_KEY=re_xxx
```
  </action>
  <verify>
    <automated>cd /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal && grep -l "createBrowserClient" src/lib/supabase/client.ts && grep -l "updateSession" src/lib/supabase/middleware.ts && grep -l "colors" src/lib/design-tokens.ts && bun run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/design-tokens.ts` contient `export const colors`
    - `src/lib/design-tokens.ts` contient `export const fonts`
    - `src/lib/design-tokens.ts` contient `export const spacing`
    - `src/lib/design-tokens.ts` contient `export default tokens`
    - `src/lib/supabase/client.ts` contient `createBrowserClient`
    - `src/lib/supabase/client.ts` contient `NEXT_PUBLIC_SUPABASE_URL`
    - `src/lib/supabase/server.ts` contient `createServerClient`
    - `src/lib/supabase/server.ts` contient `getAll()` (pas `get()`)
    - `src/lib/supabase/middleware.ts` contient `updateSession`
    - `src/lib/supabase/middleware.ts` contient `getUser()` (pas `getSession()`)
    - `src/app/layout.tsx` contient `DM_Sans` from `next/font/google`
    - `src/app/layout.tsx` contient `Cal Sans` (lien CDN)
    - `src/styles/globals.css` contient `--color-accent: #4f46e5`
    - `src/styles/globals.css` contient `--gradient-login-panel`
    - `.env.local.example` contient `SUPABASE_SERVICE_ROLE_KEY`
    - `.env.local.example` ne contient PAS `NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY`
    - `bun run build` sort sans erreur TypeScript
  </acceptance_criteria>
  <done>Design tokens copiés verbatim, 3 clients Supabase créés (browser/server/middleware), polices chargées, variables CSS déclarées, `.env.local.example` documenté.</done>
</task>

</tasks>

<verification>
`bun run dev` démarre sur localhost:3000 sans erreur. `bun run build` sort clean. Les fichiers clients Supabase existent avec les patterns corrects (`getAll`/`setAll`, `getUser()`).
</verification>

<success_criteria>
- Next.js App Router fonctionnel avec `src/` activé
- Design tokens dans `src/lib/design-tokens.ts` — copie exacte du draft
- Cal Sans + DM Sans chargées dans le layout
- 3 clients Supabase prêts: client.ts / server.ts / supabase/middleware.ts
- Variables CSS tokens dans globals.css
- `bun run build` propre
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentification-rbac/02-03-SUMMARY.md`
</output>
