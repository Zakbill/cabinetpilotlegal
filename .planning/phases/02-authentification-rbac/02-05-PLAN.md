---
phase: 02-authentification-rbac
plan: 05
type: execute
wave: 2
depends_on: [02-03]
files_modified:
  - src/app/login/page.tsx
  - src/app/login/actions.ts
  - src/components/auth/LoginForm.tsx
  - src/components/auth/ConfirmationScreen.tsx
autonomous: true
requirements: [AUTH-01]

must_haves:
  truths:
    - "L'utilisateur peut saisir son e-mail et recevoir un magic link"
    - "Après envoi, la page affiche l'écran de confirmation sans navigation (state change in-page)"
    - "Les erreurs (e-mail invalide, rate limit) sont affichées en français sous le champ"
    - "La page est split-screen: formulaire à gauche, gradient indigo à droite"
  artifacts:
    - path: src/app/login/page.tsx
      provides: "Route /login avec split-screen layout"
    - path: src/app/login/actions.ts
      provides: "Server Action sendMagicLink"
      exports: [sendMagicLink]
    - path: src/components/auth/LoginForm.tsx
      provides: "Formulaire magic link (Client Component)"
    - path: src/components/auth/ConfirmationScreen.tsx
      provides: "Écran confirmation post-envoi"
  key_links:
    - from: src/components/auth/LoginForm.tsx
      to: src/app/login/actions.ts
      via: "import { sendMagicLink } from '@/app/login/actions'"
      pattern: "sendMagicLink"
    - from: src/app/login/actions.ts
      to: supabase.auth.signInWithOtp
      via: "supabase.auth.signInWithOtp({ email, options: { emailRedirectTo } })"
      pattern: "signInWithOtp"
---

<objective>
Page de login split-screen avec formulaire magic link et écran de confirmation in-page, conforme au contrat UI-SPEC.

Purpose: Point d'entrée de toute l'application. AUTH-01 complet — aucun mot de passe, uniquement magic link Supabase via Resend.
Output: Page `/login` fonctionnelle avec Server Action + Client Components UI conformes au design contract.
</objective>

<execution_context>
@/Users/zakariabillouche/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zakariabillouche/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authentification-rbac/02-UI-SPEC.md
@.planning/phases/02-authentification-rbac/02-RESEARCH.md
@.planning/phases/02-authentification-rbac/02-03-SUMMARY.md

<interfaces>
<!-- UI-SPEC Screen 1 — Login Page (résumé des specs exactes) -->
<!-- LEFT PANEL:
  - Background: white
  - Content max-width: 360px (var --size-login-max)
  - Logo: "CabinetPilot" — Cal Sans 18px zinc-900
  - Title: "Connexion" — 20px DM Sans semibold zinc-900
  - Subtitle: "Pas de mot de passe — connexion sécurisée par lien e-mail" — 14px DM Sans zinc-500
  - Input: shadcn <Input> label "Adresse e-mail" placeholder "vous@cabinet.fr"
  - CTA: shadcn <Button> variant="default" label "Recevoir mon lien de connexion" full-width indigo-600
  - Errors: red-500 14px sous le champ
-->
<!-- RIGHT PANEL:
  - gradient: linear-gradient(135deg, #4338ca 0%, #6d28d9 100%)
  - Headline: "Pilotez vos missions juridiques en un coup d'œil" Cal Sans 32px white
  - Body: "Suivez chaque dossier AGO, assignez vos équipes et synchronisez automatiquement depuis Pennylane." — DM Sans 16px white/80%
  - Decorative only — no interactive elements
  - Hidden below md breakpoint
-->
<!-- CONFIRMATION SCREEN (same page, state change):
  - Icon: Mail (Lucide) 32px indigo-600
  - Heading: "Vérifiez votre boîte mail" — 20px DM Sans semibold
  - Body: "Un lien de connexion a été envoyé à {email}. Le lien expire dans 1 heure." — 16px DM Sans zinc-600
  - Resend link: "Renvoyer le lien" — 14px indigo-600 text link
  - Back: "← Modifier l'adresse e-mail" — 14px zinc-500
-->

<!-- sendMagicLink Server Action pattern (RESEARCH.md §Pattern Magic Link) -->
```typescript
'use server'
// src/app/login/actions.ts
export async function sendMagicLink(email: string): Promise<{ error?: string } | { success: true }>
```

<!-- React Hook Form + Zod pattern -->
```typescript
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import * as z from 'zod'

const loginSchema = z.object({
  email: z.string().email({ message: 'Adresse e-mail invalide' }),
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server Action sendMagicLink + page login layout</name>
  <files>
    src/app/login/actions.ts
    src/app/login/page.tsx
  </files>
  <read_first>
    - .planning/phases/02-authentification-rbac/02-RESEARCH.md §Pattern 4 (signInWithOtp exact)
    - .planning/phases/02-authentification-rbac/02-UI-SPEC.md §Screen 1 (layout split-screen)
    - src/lib/supabase/server.ts (createClient server — créé Plan 03)
  </read_first>
  <action>
**Fichier 1 — `src/app/login/actions.ts`:**

```typescript
'use server'

import { createClient } from '@/lib/supabase/server'

export async function sendMagicLink(email: string): Promise<{ error?: string } | { success: true }> {
  const supabase = await createClient()

  const { error } = await supabase.auth.signInWithOtp({
    email,
    options: {
      shouldCreateUser: true,
      emailRedirectTo: `${process.env.NEXT_PUBLIC_SITE_URL}/auth/callback`,
    },
  })

  if (error) {
    // Traduire les erreurs Supabase en français
    if (error.message.includes('rate limit') || error.message.includes('too many')) {
      return { error: 'Trop de tentatives. Réessayez dans quelques minutes.' }
    }
    if (error.message.includes('invalid email') || error.message.includes('valid email')) {
      return { error: 'Adresse e-mail invalide' }
    }
    return { error: 'Une erreur est survenue. Veuillez réessayer.' }
  }

  return { success: true }
}
```

**Fichier 2 — `src/app/login/page.tsx`:**

```typescript
import { LoginForm } from '@/components/auth/LoginForm'

export default function LoginPage() {
  return (
    <div className="min-h-screen flex">
      {/* Panneau gauche — formulaire */}
      <div className="flex-1 flex items-center justify-center bg-white px-8">
        <div style={{ maxWidth: 'var(--size-login-max)', width: '100%' }}>
          {/* Logo CabinetPilot */}
          <div className="mb-8">
            <span className="font-display text-[18px] font-semibold text-zinc-900">
              CabinetPilot
            </span>
          </div>

          {/* Formulaire magic link (Client Component) */}
          <LoginForm />
        </div>
      </div>

      {/* Panneau droit — gradient indigo, caché sous md */}
      <div
        className="hidden md:flex flex-1 items-center justify-center px-12"
        style={{ background: 'var(--gradient-login-panel)' }}
      >
        <div style={{ maxWidth: '320px' }}>
          <h2 className="font-display text-[32px] font-semibold text-white leading-tight">
            Pilotez vos missions juridiques en un coup d&apos;œil
          </h2>
          <p className="mt-4 text-base text-white/80 leading-relaxed">
            Suivez chaque dossier AGO, assignez vos équipes et synchronisez automatiquement depuis Pennylane.
          </p>
        </div>
      </div>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal && ls src/app/login/actions.ts src/app/login/page.tsx && grep "signInWithOtp" src/app/login/actions.ts && grep "LoginForm" src/app/login/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/login/actions.ts` contient `'use server'`
    - `src/app/login/actions.ts` contient `signInWithOtp`
    - `src/app/login/actions.ts` contient `shouldCreateUser: true`
    - `src/app/login/actions.ts` contient `NEXT_PUBLIC_SITE_URL`
    - `src/app/login/actions.ts` contient `Trop de tentatives` (message erreur rate limit FR)
    - `src/app/login/page.tsx` contient `LoginForm`
    - `src/app/login/page.tsx` contient `var(--gradient-login-panel)`
    - `src/app/login/page.tsx` contient `var(--size-login-max)`
    - `src/app/login/page.tsx` contient `CabinetPilot` dans un `font-display`
    - `src/app/login/page.tsx` contient `hidden md:flex` (right panel responsive)
  </acceptance_criteria>
  <done>Server Action `sendMagicLink` fonctionnelle avec traduction des erreurs en français. Page login split-screen avec layout correct.</done>
</task>

<task type="auto">
  <name>Task 2: LoginForm + ConfirmationScreen Client Components</name>
  <files>
    src/components/auth/LoginForm.tsx
    src/components/auth/ConfirmationScreen.tsx
  </files>
  <read_first>
    - .planning/phases/02-authentification-rbac/02-UI-SPEC.md §Screen 1 (tous les états interactifs)
    - .planning/phases/02-authentification-rbac/02-UI-SPEC.md §Interaction States (hover/focus/error)
    - .planning/phases/02-authentification-rbac/02-UI-SPEC.md §Copywriting Contract
    - src/components/ui/button.tsx (composant shadcn généré — lire avant d'utiliser)
    - src/components/ui/input.tsx (composant shadcn généré)
    - src/components/ui/form.tsx (composant shadcn généré)
  </read_first>
  <action>
**Fichier 1 — `src/components/auth/LoginForm.tsx`:**

```typescript
'use client'

import { useState } from 'react'
import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import * as z from 'zod'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import {
  Form,
  FormControl,
  FormField,
  FormItem,
  FormLabel,
  FormMessage,
} from '@/components/ui/form'
import { sendMagicLink } from '@/app/login/actions'
import { ConfirmationScreen } from './ConfirmationScreen'

const loginSchema = z.object({
  email: z.string().email({ message: 'Adresse e-mail invalide' }),
})

type LoginFormValues = z.infer<typeof loginSchema>

export function LoginForm() {
  const [sent, setSent] = useState(false)
  const [sentEmail, setSentEmail] = useState('')
  const [serverError, setServerError] = useState<string | null>(null)

  const form = useForm<LoginFormValues>({
    resolver: zodResolver(loginSchema),
    defaultValues: { email: '' },
  })

  const onSubmit = async (values: LoginFormValues) => {
    setServerError(null)
    const result = await sendMagicLink(values.email)

    if ('error' in result) {
      setServerError(result.error!)
      return
    }

    setSentEmail(values.email)
    setSent(true)
  }

  if (sent) {
    return (
      <ConfirmationScreen
        email={sentEmail}
        onBack={() => {
          setSent(false)
          setSentEmail('')
          form.reset()
        }}
        onResend={() => sendMagicLink(sentEmail)}
      />
    )
  }

  return (
    <Form {...form}>
      <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-5">
        <div>
          <h1 className="text-[20px] font-semibold text-zinc-900">Connexion</h1>
          <p className="mt-1 text-sm text-zinc-500">
            Pas de mot de passe — connexion sécurisée par lien e-mail
          </p>
        </div>

        <FormField
          control={form.control}
          name="email"
          render={({ field }) => (
            <FormItem>
              <FormLabel className="text-sm font-normal text-zinc-700">
                Adresse e-mail
              </FormLabel>
              <FormControl>
                <Input
                  type="email"
                  placeholder="vous@cabinet.fr"
                  className="h-12 border-zinc-200 focus-visible:ring-2 focus-visible:ring-indigo-600"
                  {...field}
                />
              </FormControl>
              <FormMessage className="text-sm text-red-500" />
            </FormItem>
          )}
        />

        {/* Erreur serveur (rate limit, etc.) */}
        {serverError && (
          <p className="text-sm text-red-500" role="alert">
            {serverError}
          </p>
        )}

        <Button
          type="submit"
          className="w-full h-12 bg-indigo-600 hover:bg-indigo-700 focus-visible:ring-2 focus-visible:ring-indigo-600 focus-visible:ring-offset-2"
          disabled={form.formState.isSubmitting}
        >
          {form.formState.isSubmitting
            ? 'Envoi en cours...'
            : 'Recevoir mon lien de connexion'}
        </Button>
      </form>
    </Form>
  )
}
```

**Fichier 2 — `src/components/auth/ConfirmationScreen.tsx`:**

```typescript
'use client'

import { useState } from 'react'
import { Mail } from 'lucide-react'

interface ConfirmationScreenProps {
  email: string
  onBack: () => void
  onResend: () => Promise<{ error?: string } | { success: true }>
}

export function ConfirmationScreen({ email, onBack, onResend }: ConfirmationScreenProps) {
  const [resending, setResending] = useState(false)
  const [resendMessage, setResendMessage] = useState<string | null>(null)

  const handleResend = async () => {
    setResending(true)
    setResendMessage(null)
    const result = await onResend()
    setResending(false)

    if ('error' in result) {
      setResendMessage(result.error!)
    } else {
      setResendMessage('Lien renvoyé !')
    }
  }

  return (
    <div className="space-y-5">
      {/* Icône envelope */}
      <div className="flex justify-center">
        <Mail className="h-8 w-8 text-indigo-600" aria-hidden="true" />
      </div>

      <div className="text-center space-y-2">
        <h1 className="text-[20px] font-semibold text-zinc-900">
          Vérifiez votre boîte mail
        </h1>
        <p className="text-base text-zinc-600">
          Un lien de connexion a été envoyé à{' '}
          <span className="font-semibold text-zinc-900">{email}</span>.
          Le lien expire dans 1 heure.
        </p>
      </div>

      <div className="flex flex-col items-center gap-2">
        <button
          type="button"
          onClick={handleResend}
          disabled={resending}
          className="text-sm text-indigo-600 hover:underline focus:outline-none focus:ring-1 focus:ring-zinc-400 disabled:opacity-50"
        >
          {resending ? 'Envoi...' : 'Renvoyer le lien'}
        </button>

        {resendMessage && (
          <p
            className={`text-sm ${resendMessage.includes('!') ? 'text-green-600' : 'text-red-500'}`}
            role="status"
          >
            {resendMessage}
          </p>
        )}
      </div>

      <button
        type="button"
        onClick={onBack}
        className="text-sm text-zinc-500 hover:text-zinc-700 focus:outline-none focus:ring-1 focus:ring-zinc-400 w-full text-center"
      >
        ← Modifier l&apos;adresse e-mail
      </button>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd /Users/zakariabillouche/Documents/Coding/Cabinetpilotlegal && ls src/components/auth/LoginForm.tsx src/components/auth/ConfirmationScreen.tsx && grep "'use client'" src/components/auth/LoginForm.tsx && grep "sendMagicLink" src/components/auth/LoginForm.tsx && bun run build 2>&1 | grep -E "error TS|✓ Compiled" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/auth/LoginForm.tsx` contient `'use client'`
    - `src/components/auth/LoginForm.tsx` contient `zodResolver`
    - `src/components/auth/LoginForm.tsx` contient `sendMagicLink`
    - `src/components/auth/LoginForm.tsx` contient `ConfirmationScreen` (import + usage)
    - `src/components/auth/LoginForm.tsx` contient `Recevoir mon lien de connexion` (copie exacte UI-SPEC)
    - `src/components/auth/LoginForm.tsx` contient `h-12` (hauteur 48px = WCAG touch target)
    - `src/components/auth/LoginForm.tsx` contient `focus-visible:ring-2 focus-visible:ring-indigo-600`
    - `src/components/auth/ConfirmationScreen.tsx` contient `'use client'`
    - `src/components/auth/ConfirmationScreen.tsx` contient `Vérifiez votre boîte mail`
    - `src/components/auth/ConfirmationScreen.tsx` contient `Renvoyer le lien`
    - `src/components/auth/ConfirmationScreen.tsx` contient `Modifier l` (← Modifier l'adresse e-mail)
    - `bun run build` sans erreur TypeScript sur ces fichiers
  </acceptance_criteria>
  <done>LoginForm avec react-hook-form + zod + Server Action. ConfirmationScreen avec resend + back. Toute la copie française conforme à UI-SPEC. Transition in-page (pas de navigation) via state React.</done>
</task>

</tasks>

<verification>
`bun run build` propre. Page `/login` accessible. `LoginForm` et `ConfirmationScreen` compilent sans erreur. Le champ email se valide côté client (zod) et côté serveur (Supabase). Les erreurs sont en français.
</verification>

<success_criteria>
- Page `/login` split-screen : formulaire gauche, gradient droit, responsive (panneau droit caché sous md)
- Magic link envoyé via `supabase.auth.signInWithOtp` avec `emailRedirectTo` vers `/auth/callback`
- Après envoi : écran confirmation in-page avec email affiché, lien renvoyer, retour formulaire
- Erreurs en français : "Adresse e-mail invalide", "Trop de tentatives"
- Tous les éléments interactifs à 48px de hauteur (WCAG 2.5.5)
- `bun run build` propre
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentification-rbac/02-05-SUMMARY.md`
</output>
